# Review #15 — M14
Reviewer: Claude Opus 4.8 | Data: 2026-06-15
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 6 wymiarów: save-migration-v3, comms-split-routing-scope, notifications-toast-dnd, unread-badge-zeroing, devfloor-split-windows, hardrules-tests-sweep + weryfikacja adwersarialna + sweep, 12 agentów; reviewer własnoręcznie potwierdził migrację v2→v3, zielony check, brak P0):
  DoD: `npm run check` zielony (tsc + eslint + prettier + 117× vitest + validate PASS 58 plików). Zakres M14 = podział ekranów na okna + rozbicie comms docka na Chat/Mail/Feed + powiadomienia narożne; NIE rusza treści/triggerów fabuły (05) ani ekonomii (02/03).
  CO DZIAŁA (zweryfikowane): SAVE — SAVE_VERSION 2→3, migracja v2→v3 (migrations.ts:30-38 + migrateM14Ui:96-116) dodaje settings.doNotDisturb i konwertuje stare okno `comms` → `chat` (`{...comms, appId:"chat"}`), domyślne chat/mail/feed bez utraty danych; test save.test.ts:104-120 migruje fixture v2 (repaired=true, windows.chat.appId="chat", windows.comms=undefined) + round-trip v3 z createDefaultGameState repaired=false. Comms split: AppId comms USUNIĘTY, dodane chat/mail/feed; routing kanału `matchesReadChannel` (story.ts:298-308: feed=feed|system|news, chat/mail izolowane); treść/triggery eventów BEZ ZMIAN (brak znalezisk scope — regresja Aktu 0-1 nienaruszona). Powiadomienia: toast „New mail/chat/feed" NIE pokazuje treści wiadomości (AC spełnione — brak wycieku treści), klik → otwiera apkę, reuse Toast, bleep „message", DnD w settings. Unread per kanał (getUnreadStoryCount) + zerowanie przy otwarciu (openApp→markCommsAppRead, main.ts:1016-1026) — KOD POPRAWNY. Dev Floor rozbity na Agents/Hardware/Upgrades (brak znalezisk o utracie funkcji). Brak runtime deps.
  GŁÓWNE LUKI: (P1) AC „unread liczone + zerowane po otwarciu" nietestowane na ścieżce per-kanał (chat/mail/feed) — tylko legacy „archive". (P2) martwy markCommsRead, osierocone klucze i18n po starym docku. BRAK P0. -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P1-1** | `tests/story.test.ts:115-123` (+ `tests/render.test.ts:70-102`) — AC M14 „unread liczone poprawnie i zerowane po otwarciu" NIE jest testowane na faktycznej ścieżce M14 (per-kanał chat/mail/feed): jedyny test zerowania używa agregatu „archive" (`getUnreadStoryCount(state)` bez argumentu + `markStoryInboxRead(state,"archive")`, który w story.ts:298-300 short-circuituje na true dla każdego eventu), a test M14 w render.test.ts wstrzykuje STATYCZNE `unreadByChannel:{chat:1,...}` i nigdy nie woła getUnreadStoryCount ani ścieżki openApp→markCommsAppRead→markStoryInboxRead(channel); niepokryta jest też reguła feed=feed|system|news (story.ts:303-305) — dlaczego: 08 M14 AC; kod POPRAWNY (story.ts:116-135/298-308, main.ts:1016-1026), ale nazwany AC jest nieweryfikowany → missing required test (P1); regresja (otwarcie Chat zeruje też Mail/Feed; getUnreadStoryCount ignoruje argument kanału; event system/news podwójnie liczony pod feed) przeszłaby zielone CI — fix: dodaj testy: getUnreadStoryCount(state,'chat'|'mail'|'feed') respektuje matchesReadChannel (feed liczy feed|system|news raz), oraz markStoryInboxRead(state,'chat') zeruje TYLKO chat.
  - fix-note: Added `tests/story.test.ts` regression covering per-channel unread counts for chat/mail/feed, feed aggregation of feed/system/news, and `markStoryInboxRead(state, "chat")` clearing only chat. Covered by `npm run check`.

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/main.ts:431` (+ decl `src/ui/render.ts:423`) — akcja `markCommsRead` jest martwa: nigdy nie wołana z ui/ (grep src/ui: tylko deklaracja interfejsu + stub w teście); zerowanie „po otwarciu" realizuje INNA funkcja `markCommsAppRead` (main.ts:1016-1026, wpięta w openApp/toast-onClick/story:message handler) — dlaczego: 08 M14 (badge unread + zerowanie); AC spełnione przez markCommsAppRead, więc to martwy kod (analogiczne do „orphaned i18n key" = P2), nie błąd runtime — fix: usuń osieroconą akcję markCommsRead z AppActions + main.ts (albo podłącz, jeśli intencjonalna).
  - fix-note: Removed the dead `markCommsRead` action from `AppActions`, `main.ts`, and the render test action stub. Covered by `npm run check`.
- [x] **P2-2** | `src/i18n/en.json:536,542,544,594` — osierocone klucze i18n po usuniętym docku §3: `ui.comms.archive` (536), `ui.comms.tabAria` (542), `ui.comms.title` (544), `ui.app.comms` (594); po M14 dock nie renderuje archiwum/zakładek ani panelu „Comms", a nie ma AppId „comms" — konstrukcje dynamiczne `ui.comms.${channel}` (render.ts:2867/2876) i `ui.app.${appId}` (render.ts:1406/2772) rozwiązują się tylko do chat/mail/feed, więc te 4 klucze są nieosiągalne (grep poza en.json: 0 referencji) — dlaczego: 08 M14 (rozbicie comms docka) + 06 §14; validate.ts nie wykrywa orphanów → bloat — fix: usuń 4 martwe klucze z en.json.
  - fix-note: Removed the four orphaned comms-dock i18n keys from `src/i18n/en.json`. Covered by `npm run check`.

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
_(brak)_

## Runda 2 — weryfikacja (wypełnia reviewer)
(`npm run check` zielony: 118/118 vitest, validate PASS 58 plików.)
- Naprawy zweryfikowane:
  - **P1-1 OK** — nowy test per-kanał (story.test.ts:130-146): inbox z eventami chat/mail/feed/system/news; asercje `getUnreadStoryCount(state,"chat")=1`, `"mail")=1`, `"feed")=3` (feed agreguje feed|system|news RAZ) — pokrywa matchesReadChannel; `markStoryInboxRead(state,"chat")` → chat=0 ALE mail=1/feed=3 nietknięte (izolacja per-kanał); `markStoryInboxRead(state,"feed")` → feed=0. Dokładnie testuje AC „unread liczone + zerowane po otwarciu" na ścieżce M14 (regresja cross-zerowania / złego feed-matchingu byłaby teraz wychwycona).
  - **P2-1 OK** — `markCommsRead` USUNIĘTY z AppActions, main.ts i stuba testowego (grep src+tests: 0 referencji); ścieżka zerowania (markCommsAppRead) nietknięta.
  - **P2-2 OK** — 4 osierocone klucze (ui.comms.archive/tabAria/title, ui.app.comms) usunięte z en.json (grep: 0).
- Nowe P0 wprowadzone poprawkami: brak. Sprawdzone: usunięcie markCommsRead nie zostawia wiszącej referencji (tsc zielony, 0 wystąpień); usunięte klucze i18n były nieosiągalne (brak lookupu je rozwiązującego); nowy test to czysty dodatek bez zmian kodu produkcyjnego. 118/118 testów + validate PASS.
- Decyzja: DONE — jedyny blocker (P1-1: brak testu per-kanał AC unread) rozwiązany i zweryfikowany (test pokrywa licznik per-kanał + izolowane zerowanie + feed=feed|system|news), P2-1/P2-2 naprawione (martwy kod + orphan keys usunięte), poprawki bez nowych P0. M14 (podział apek + Chat/Mail/Feed + powiadomienia) spełnia AC; migracja v2→v3, brak wycieku treści w toaście, niezmienione triggery fabuły i podział Dev Floor zweryfikowane w rundzie 1.

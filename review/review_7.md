# Review #7 — M6
Reviewer: Claude Opus 4.8 | Data: 2026-06-14
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 5 wymiarów + weryfikacja adwersarialna + sweep, 22 agenty):
  DoD: `npm run check` zielony (tsc + eslint + prettier + 54× vitest + validate PASS).
  Liczby: WSZYSTKIE 30 węzłów research (data/research.ts) == 09 §6 dokładnie (koszty RP 2,3,5,8,12,18,25,35,50,70 + efekty per gałąź); WSZYSTKIE 36 upgrade'ów (data/upgrades.ts) == 09 §5 dokładnie (koszt/efekt/unlock). Zweryfikowane 1:1.
  Condition (07 §5): unlocks.ts checkCondition obsługuje teraz wszystkie pola (w tym nowe generatorGte/debtGte/hypeMaxGte/upgrade/research/hypeGte); ujednolicony typ w data/conditions.ts. Sierota z review_6 (P2-1) ROZWIĄZANA — upgrade'y używają checkCondition. hypeMaxGte działa (hype.ts recordMaxHype zapisuje stat "hype.max").
  Folding (production.ts recompute): clickSynergy/flowDuration/offlineCapH/hypeCap/floor/tau/boardSlots/bugPenalty/autoPromptRate = Math.max (poprawnie bierze najwyższy odblokowany); quality = suma z clampem do C.QUALITY_MAX (0.9); locMultiplier mnożone do multipliers.research. POPRAWNE.
  AC część 1 ("efekty przez DerivedCache, zero liczenia w tick"): konsumenci czytają z cache — debt.ts (cache.debt.factor/quality/bugChanceCap/Multiplier/bugPenalty/refactorMultiplier), prompt.ts (cache.click.multiplier/synergy/flowGain/flowDurationS), hype.ts (cache.hype.cap/floor/tauS/shipMultiplier), projects.ts (cache.project.payout/revenue/buildTime/boardSlots), compute.ts (cache.compute.useMultiplier — computeUsed przeliczany z owned×effectiveUse). Wszystkie znalezione efekty SĄ konsumowane (jedyny wyjątek: qaMultiplier — P2-1).
  Hard rules: brak zależności runtime; data/ czyste (research/upgrades/conditions to dane/typy); systems/ bez DOM; Big dla kosztów/loc/money. GameState bez nowego pola persistowanego (research/upgrades Sety + automation Record istnieją od M2) → SAVE_VERSION bez zmian (OK).
  AC część 2 ("automatyzacje mają toggle w UI i działają"): auto-prompt/auto-fix OK; auto-buy MA dwa defekty → P1-1, P1-2. -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P1-1** | `src/ui/render.ts:1207` — toggle auto-buy dla generatorów odblokowanych PO mount nigdy się nie renderuje: `createAutomationToggle` woła się tylko raz w `createDevFloorScreen` (`:565-567`) przy mount, a ścieżka per-klatka `updateAutomationToggle` (`:1207-1212`) robi `automationToggles.get(view.id); if (undefined) return;` i NIGDY nie tworzy nowego węzła; `getAutomationToggles` (`automation.ts:55-66`) emituje `autoBuy:<gen>` tylko dla odblokowanych generatorów, a przy świeżym mount odblokowany jest tylko g_autocomplete → dlaczego: AC M6 „automatyzacje mają toggle w UI i działają” + 02 §7 poz.2 (auto-buy per-typ); w jednej sesji (bez reloadu) gracz kupuje g_autocomplete→g_parrot→g_macro i odblokowuje autoBuy, ale checkboxy auto-buy dla g_parrot/g_macro nie pojawiają się nigdy (kontrast: `syncUpgradeRows` `:1177-1184` TWORZY wiersze on-sync) → fix: w `updateAutomationToggles` twórz brakujący węzeł toggle on-sync (jak upgrade rows), zamiast pomijać nieznane id.
  - fix-note: Fixed by replacing one-shot automation updates with sync creation for missing toggle nodes in `src/ui/render.ts`; regression covered by `tests/render.test.ts` ("detects automation toggles that unlock after the initial mount").
- [x] **P1-2** | `src/systems/automation.ts:118` — auto-buy „keep 10% cash” kompounduje się do ~0.1^N przy N włączonych typach: `spendBudget = money × (1 − AUTO_BUY_KEEP_CASH_RATIO)` jest liczony W PĘTLI po generatorach z BIEŻĄCEJ (już pomniejszonej) gotówki, a `buyGenerator` honoruje `spendBudget` (`production.ts:341,354`) i odejmuje koszt → każdy kolejny typ zostawia 10% tego, co poprzedni zostawił → dlaczego: 02 §7 poz.2 „keep 10% cash” (bufor bezpieczeństwa) — przy 2 typach gotówka spada do ~1%, przy 3 do ~0.1% zamiast płaskich 10% → fix: policz `spendBudget` RAZ przed pętlą (z gotówki na początku przebiegu) albo egzekwuj jeden globalny próg 10% na cały przebieg auto-buy. (Patrz też PLAN-ISSUE.)
  - fix-note: Fixed by computing a single reserve floor at the start of `tickAutoBuy` and spending only money above that global floor; regression covered by `tests/m6-research-upgrades-automation.test.ts` multi-type auto-buy case.

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/systems/production.ts:260` — `cache.project.qaMultiplier` (z r_q6 „QA bots ×2”, `research.ts:199`) jest foldowany do cache, ale ŻADEN konsument go nie czyta (grep: tylko zapis) → r_q6 (18 RP) jest bezczynny — dlaczego: konsument (pasywna redukcja debt przez QA bota g_qa_bot, 03 §5.3) to przyszły system (g_qa_bot to E2, niezaimplementowany); AC „efekty przez cache” literalnie spełnione; arguably zły bucket (`cache.project` vs `cache.debt`) — fix: oznacz jako latentny do czasu implementacji QA bota, ewentualnie przenieś do cache.debt.
  - fix-note: → backlog
- [x] **P2-2** | `src/systems/automation.ts:119` — `tickAutoBuy` woła `buyGenerator` → `recomputeDerivedCache` (`production.ts:360`) WEWNĄTRZ ticku przy każdym udanym zakupie (pełny recompute: 8 obiektów + iteracja 36 upgrade + 30 research + per-generator) — dlaczego: 07 §9 / AC „zero liczenia w tick”; bramkowane udanym zakupem (rzadkie po early game, samoograniczające przez wykładniczy koszt) i recompute jest potrzebny między sekwencyjnymi zakupami (świeży compute.available) — fix: rozważ jeden recompute po pętli auto-buy zamiast per-zakup.
  - fix-note: → backlog
- [x] **P2-3** | `src/systems/production.ts:288` — `cache.generatorEntries[id].maxAffordable` (i `computeUse` `:285`) jest zapisywane przy KAŻDYM recompute (woła `getGeneratorMaxAffordable` → `Big.maxAffordable`, log + pętla korekcyjna per generator), ale nigdy nie czytane (createGeneratorView `main.ts:380` i buyGenerator liczą je NA ŻYWO) — dlaczego: 07 §9 „nie licz w hot path / cache tylko to, co konsumowane”; martwa praca w (per-tick) recompute — fix: usuń pola `maxAffordable`/`computeUse` z generatorEntries i polegaj na liczeniu na żywo.
  - fix-note: → backlog
- [x] **P2-4** | `src/core/save.ts:184` — `res.computeUsed` naprawiane z `{ integer: true }`, ale recompute zapisuje UŁAMKOWY computeUsed gdy posiadany jest upgrade computeUseMultiplier (u_undervolting 0.9 itd. → owned×computeUse×0.9) → przy load `repairNumber` odrzuca ułamek, ustawia `repaired=true`, ostrzega i wymusza dodatkowy zapis przy starcie (recompute i tak naprawia wartość) — dlaczego: niespójność (main.ts:687 formatCompute już zakłada ułamkowy compute); benign/self-healing, brak utraty danych — fix: usuń `integer: true` z naprawy computeUsed (zostaw nonNegative).
  - fix-note: Removed `integer: true` from `res.computeUsed` repair so fractional effective compute survives load repair.
- [x] **P2-5** | `src/main.ts:113-124` — `bug:spawned`/`bug:fixed` są w pętli `markStructuralChanged` (pełny recompute), choć żadna wartość w cache nie zależy od tego, które produkty są zabugowane (bugPenalty stosowany NA ŻYWO w `getProjectIncomeRate`) → nadmiarowa inwalidacja cache — dlaczego: 07 §9 (recompute tylko gdy zmienia się wejście cache); w praktyce subsumowane przez znany per-tick recompute długu (cache i tak brudny co tick), więc realny koszt ~0 — fix: routuj bug:* przez markResourceChanged (tylko view).
  - fix-note: Routed `bug:spawned` and `bug:fixed` through `markResourceChanged` instead of structural cache invalidation.
- [x] **P2-6** | `tests/m6-research-upgrades-automation.test.ts:76-89` — słaby test: (a) test auto-buy włącza tylko 1 typ → maskuje P1-2 (kompounding keep-cash); (b) tylko 2 z ~33 rodzajów efektów mają asercję na poziomie konsumenta (locMultiplier, clickMultiplier) → efekt bezczynny (np. qaMultiplier) przechodzi zielony — dlaczego: P2 „weak test” vs AC (obie części) — fix: dodaj test 2+ typów auto-buy (sprawdź globalny próg 10%) i asercje konsumenta dla payout/revenue/hype/debt/buildTime/generator-mult.
  - fix-note: Added the 2-type auto-buy regression for P1-2; broader consumer-effect matrix is → backlog.
- [x] **P2-7** | `10`, `no`, `{const`, `ruvector.db` (root repo) — pliki-śmieci w drzewie roboczym (pomyłki powłoki / artefakt narzędzia); przy `git add .` trafią do commitu M6 — dlaczego: higiena zakresu (jak wcześniejsze `{`/`308'` z M2/M5) — fix: usuń pliki przed commitem (i rozważ wpis w .gitignore dla `ruvector.db`).
  - fix-note: Removed the four named root junk files and added `ruvector.db` to `.gitignore`.

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
<!-- pozycje, z którymi Codex się nie zgadza: id znaleziska + uzasadnienie z cytatem z planu -->
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
- 02 §7 poz.2 „Auto-buy agentów (per typ, toggle, 'keep 10% cash')” nie definiuje semantyki przy WIELU włączonych typach (10% per typ vs jeden globalny próg 10% na przebieg). P1-2 zakłada intencję „globalny bufor 10%”; jeśli intencją jest „10% per typ”, to zachowanie kodu jest literalnie zgodne i P1-2 należy zdegradować — proszę o doprecyzowanie w planie.

## Runda 2 — weryfikacja (wypełnia reviewer)
- Naprawy zweryfikowane:
  - **P1-1 OK** — `syncAutomationToggles` (`render.ts:1201-1221`) liczy `missingIds` (`getMissingAutomationToggleIds`) i `createAutomationToggle` tworzy brakujące węzły on-sync (tylko dla nowo-odblokowanych id, jak `syncUpgradeRows` — bez tworzenia DOM co klatkę), potem aktualizuje wszystkie. Toggle auto-buy dla generatorów odblokowanych po mount pojawia się. Test regresyjny w `tests/render.test.ts`.
  - **P1-2 OK** — `reserveFloor = money × AUTO_BUY_KEEP_CASH_RATIO` liczony RAZ przed pętlą (`automation.ts:110`); `spendBudget = money − reserveFloor` per iteracja z `break` gdy ≤0 → globalny próg 10% trzyma się (brak kompoundingu: $1e6 → zostaje ~$1e5 niezależnie od liczby typów). Test 2-typowy w `tests/m6-research-upgrades-automation.test.ts`.
  - P2-4 OK (`save.ts:184` naprawa computeUsed bez `integer:true`, ułamek przechodzi, nonNegative zachowane); P2-5 OK (`main.ts:118-123` `bug:fixed`/`bug:spawned` → `markResourceChanged`, bug:* usunięte z listy strukturalnej); P2-6 OK (dodano test 2-typowy; szersza matryca konsumentów → backlog); P2-7 OK (4 nazwane pliki-śmieci usunięte + `ruvector.db` w .gitignore). P2-1/P2-2/P2-3 → backlog (świadomie, P2).
- Nowe P0 wprowadzone poprawkami: brak. Sprawdzone: P1-1 create-on-sync nie tworzy DOM co klatkę (tylko brakujące id); P1-2 floor globalny poprawny (kolejność iteracji daje priorytet 1. generatorowi, ale to nie regresja ani P0); P2-5 — żadna wartość w cache nie zależy od stanu bugów (bugPenalty per-produkt liczony na żywo w `getProjectIncomeRate`), income/badge nadal odświeżane przez viewDirty → brak regresji; P2-4 nonNegative zachowane. `npm run check` 55/55 zielone.
  - Uwaga (nieblokująca, P2/higiena, nie nowy P0): nazwane pliki-śmieci z P2-7 usunięte, ale pojawiły się NOWE artefakty powłoki w korzeniu (`({`, `x.verdict`) — powtarzający się problem; do zamiecenia przed commitem.
- Decyzja: DONE — oba blockery (P1-1, P1-2) rozwiązane i zweryfikowane w kodzie, P2 naprawione (4/5/6/7) lub w backlogu (1/2/3), poprawki bez nowych P0.

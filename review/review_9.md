# Review #9 — M8
Reviewer: Claude Opus 4.8 | Data: 2026-06-14
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: APPROVED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 5 wymiarów: effect-consumption, rewrite-reset, ui-forecast, save-scope-tests, sweep + weryfikacja adwersarialna, 18 agentów):
  DoD: `npm run check` zielony (tsc + eslint + prettier + 64× vitest + validate PASS 49 plików).
  AC1 (pełny cykl rewrite→insight→węzły→szybszy restart): performRewrite (prestige.ts:154-218) robi selektywny reset zgodny z 04 §1 — zeruje loc/money/debt/hype/computeUsed/era/generators/hardware/upgrades/projects/bugs/flow/automation; ZACHOWUJE insight/rp/research/equity/insightNodes/story/lifetime (zweryfikowane vs 04 §1). insightGain = max(0, floor((locSinceExit/INSIGHT_DIV)^INSIGHT_EXP) − insightSinceExit) == 04 §2. 25 węzłów Insight == 09 §8 dokładnie (i_v1..i_v8, i_c1..i_c8, i_k1..i_k8, i_core_automation; koszty 5→500, requires-łańcuch, requiresAnyTierGte:3 dla core). Efekty foldowane do cache przez applyInsightEffect (production.ts:561-592). 23 z 25 węzłów KONSUMOWANE (flowMultiplier i_v6→prompt.ts:75 cache.click; generatorCostMultiplier i_v3→production.ts:384-409; refactorInstant i_k6→projects.ts:231; payout/revenue/boardSlots/qualityAdd/debtFactor/hypeTau/offlineCap/startMoney/startEra/startGenerator/startMoneyRatio/keepAutomation/bugPenalty — wszystkie mają konsumenta). 2 węzły martwe (P2-1, konsument w future-scope).
  AC2 (prognoza zgodna z faktem): prestige.test.ts:40-108 asercja pole-po-polu — preview.lost*/startMoney/lostAgents=12/startMoney=600 (i_c1 500 + i_c4 1%×10000) MATCH realnemu stanowi po performRewrite (loc 0, money=startMoney, generators g_autocomplete→5 [i_v1], hardware {}, computeCap 6, research KEPT, automation {}, story KEPT, booting). Model prognozy KOMPLETNY i zgodny z faktem. (Luka tylko w warstwie widoku — P2-3.)
  AC3 (save przed/po migruje): prestige.test.ts:155-173 round-trip post-REWRITE → repaired=false, insight/insightNodes/rewrites/generators zachowane. SAVE_VERSION bez zmian — brak nowego pola GameState w M8 (pola prestige/insight w kształcie sprzed M8), backward-compat przez repair-not-reject. Wymiar save-scope potwierdził: brak brakującej migracji.
  Hard rules OK: brak runtime deps; data/prestige.ts czyste dane; systems/prestige.ts bez DOM; ui/render.ts bez game-math (czyta preview); teksty przez i18n; Big tam gdzie >1e15.
  Brak P0/P1 — M8 funkcjonalnie spełnia AC. Wszystkie znaleziska P2 (advisory) + 2 PLAN-ISSUE. -->

## MUST FIX (P0/P1 — blokuje merge)
_(brak)_

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/data/prestige.ts:183` (i_c7 `goldenClientChance`) + `:235` (i_k4 `qaMultiplier`) — dwa węzły Insight są MARTWE: efekt jest foldowany do akumulatora i zapisywany do cache (`production.ts:289`/`567-568` golden; `:291`/`585-586` qa), ale NIKT nie czyta `cache.project.goldenClientChance` ani `cache.project.qaMultiplier` (grep całego src potwierdza — brak czytelnika); kupienie i_c7 (320 insight) lub i_k4 (40 insight) nie daje graczowi nic — dlaczego: AC plan/08 linia 49 „węzły działają" + 09 §8 definiuje te efekty; mechaniki konsumujące (rzadkie „golden" oferty z board, efektywność QA-bota g_qa_bot z 09 tab. E2) NIE są w zakresie M8 i nie istnieją w src — to ta sama klasa co znany martwy `qaMultiplier` (research r_q6, P2 z M6: martwy efekt, którego konsument jest w future-scope) — fix: zablokuj/ukryj te dwa węzły do czasu wdrożenia mechanik konsumujących, albo przepnij efekt na już-podłączony kind, albo (z planistą) przesuń je do milestone'u który dostarcza konsumenta.
  - fix-note: → backlog (requires new consumers for golden project offers and QA-bot effectiveness, beyond the <10-line P2 allowance and outside this review-fix scope).
- [x] **P2-2** | `src/systems/prestige.ts:200` — performRewrite NIE czyści `state.stats` (zapisuje tylko klucz boot); ulotne klucze `bugs.spawnedAt.<productId>` (ustawiane `debt.ts:149`, kasowane tylko przy fixie `debt.ts:61`) zostają jako rezydua, gdy rewrite następuje przy produkcie z niezałatanym bugiem (do BUG_MAX_ACTIVE=5 osieroconych kluczy) — dlaczego: 04 §1 lista resetu pomija `stats` celowo (projects.shipped/hype.max/achievements/story to trwałe liczniki — OK, by-design), ale `bugs.spawnedAt.*` to stan biegu, nie trwały licznik; wpływ ograniczony (ograniczone uniwersum product-id, brak crasha/błędnego autofixu — `tickAutoFix` iteruje `state.bugs`=[], `trySpawnBug` nadpisuje klucz) → drobna higiena save'a — fix: przy rewrite usuń klucze prefiksu `bugs.spawnedAt.` ze `state.stats` (zostaw trwałe liczniki).
  - fix-note: Cleared `bugs.spawnedAt.*` keys during `performRewrite` while preserving durable stats; covered by `tests/prestige.test.ts` forecast/reset test.
- [x] **P2-3** | `src/main.ts:660-678` (createRewritePreviewView) + `src/ui/render.ts:145-162`/`793-807` — prognoza w UI NIE pokazuje `preview.startGenerators`: widok mapuje startMoney i startEra, ale gubi przewagę startowych agentów (typ RewritePreviewView nie ma pola startGenerators), a sekcja strat pokazuje `lostAgents=12` brutto; gracz z i_v1 widzi „tracisz 12 agentów" bez informacji, że zacznie z 5 — dlaczego: 04 §6 (lose/gain/forecast) + AC linia 49 „prognoza zgodna z faktem"; model prognozy (RewritePreview) jest KOMPLETNY i zgodny z faktem (prestige.test.ts:96 vs :83) — to wyłącznie pominięcie wiersza w warstwie widoku, każda WYŚWIETLONA wartość jest poprawna → completeness, nie incorrectness — fix: dodaj pole startGenerators do RewritePreviewView + wiersz „agenci startowi" w sekcji gain (i18n).
  - fix-note: Added `startGenerators` to `RewritePreviewView`, rendered it in the Rewrite forecast, and added i18n for the label/empty state.
- [x] **P2-4** | `src/ui/render.ts:1803` (`rewriteNodes.boot.hidden = !view.preview.booting`) — baner boot („git init --new-life") może wisieć znacznie dłużej niż REWRITE_BOOT_S=3 s: `isRewriteBooting` to czysty test `playtimeS < bootUntil`, ale nic nie planuje re-renderu na `playtimeS == bootUntil`; przy PIERWSZYM rewrite (0 węzłów → 0 startGenerators → locRate=0) tickProduction/projects/hype/debt/automation zwracają false, jedyna inwalidacja to one-shot a1_09 → baner znika dopiero przy kolejnej inwalidacji (odświeżenie board +60 s lub akcja gracza) — dlaczego: 04 §6 / 06 §4 sekwencja boot 3 s; stan wewnętrzny boot poprawny (brak utraty danych), ale wizualnie przekracza 3 s w przypadku locRate=0 — fix: oznacz widok jako brudny po przekroczeniu bootUntil (np. timer/scheduler na resztę okna boot lub flaga w pętli renderu, gdy isRewriteBooting przeszło w false).
  - fix-note: Main loop now marks the view dirty on the transition from `isRewriteBooting=true` to false, so the banner clears at the boot boundary even with zero production.
- [x] **P2-5** | `src/ui/render.ts:784-785` + `src/ui/layout.css:548-558` — deliverable „sekwencja boot" w warstwie UI to jednolinijkowy baner z animacją `comms-pulse` (glow) wewnątrz siatki forecast, NIE pełnoekranowa sekwencja 2-3 s (spadające linie repo, `git init`, mrugający kursor) — dlaczego: 06 linia 86 „pełnoekranowa sekwencja 2-3 s … Najważniejsza animacja w grze" / 04 linia 95; część systemowa boot (REWRITE_BOOT_S, isRewriteBooting, 3 s stanu) JEST wdrożona i konsumowana, AC (linia 49) nie testuje animacji → to luka „juice"/polish, nie naruszenie funkcjonalne — fix: dodaj pełnoekranową animację boot (pomijalną; prefers-reduced-motion zostaw do M11 wg jego DoD).
  - fix-note: → backlog (full-screen boot animation is larger than the <10-line P2 allowance and belongs with polish/juice work).
- [x] **P2-6** | `tests/prestige.test.ts:40-173` — brak testu negatywnej ścieżki progu REWRITE: każde wywołanie performRewrite jest ustawione tak, by przejść bramkę (`availableInsight >= requiredInsight`), więc nikt nie sprawdza, że rewrite zwraca `{ok:false, reason:"threshold"}` przy availableInsight<requiredInsight ani że `REWRITE_MIN_GAIN_RATIO`=0.25 (rewrites>0) faktycznie blokuje; dodatkowo round-trip (`:155-173`) nie re-asercjonuje `isRewriteBooting(result.state)` po reloadzie ani nie ma fixtury z `prestige.rewriteBootUntil` (boot-przetrwa-reload tylko domyślnie pokryty) — dlaczego: 04 §2 (próg) + AC linia 49 (pełny cykl + save migruje); regresja odwracająca `>=` (prestige.ts:136) lub zerująca ratio przeszłaby zielone CI — fix: dodaj test bramki (ok=false przy za-małym gain, oba przypadki: pierwszy rewrite <5 i kolejny <25%) oraz asercję isRewriteBooting po deserializacji save'a mid-boot.
  - fix-note: Added negative threshold coverage for first and repeat REWRITE gates plus a mid-boot deserialization assertion in `tests/prestige.test.ts`.
- [x] **P2-7** | korzeń repo (`no`, `{const`, `agentdb.rvf`, `agentdb.rvf.lock`) — puste/narzędziowe pliki-artefakty w drzewie roboczym (z powłoki/narzędzi, nie z kodu M8); przy `git add .` trafią do commitu — dlaczego: higiena zakresu (powtarzający się problem M2/M5/M6/M7) — fix: usuń przed commitem (+ rozważ wpis w .gitignore dla `agentdb.rvf*`).
  - fix-note: Removed root tool artifacts present in this worktree (`agentdb.rvf*`, `x.verdict`, and empty shell leftovers `({`/`` ` ``) and added `.gitignore` entries for `agentdb.rvf*` and `x.verdict`.

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
- **Próg „+25% obecnego stanu" (04 §2) — niejednoznaczna baza.** Implementacja liczy próg kolejnego rewrite jako `state.lifetime.insightSinceExit * 0.25` (prestige.ts:234, test asercjonuje 1.25 dla insightSinceExit=5). „Obecny stan" w planie może oznaczać `insightSinceExit` (insight zdobyty w tym biegu) albo `res.insight` (całkowity insight) — implementacja wybrała pierwsze, jest wewnętrznie spójna i przetestowana; plan warto doprecyzować, którą bazą jest „obecny stan".
- **09 §8 specyfikuje efekty bez konsumenta w zakresie M8.** Drzewo Insight (09 §8) zawiera `goldenClientChance` (i_c7) i `qaMultiplier` (i_k4), których mechaniki konsumujące (rzadkie „golden" oferty, efektywność QA-bota) nie należą do warstwy prestiżu M8 i nie są dostarczane w żadnym wcześniejszym milestonie → drzewo M8 z założenia zawiera 2 nieaktywne węzły (źródło P2-1). Warto w planie przypisać tym węzłom milestone dostarczający konsumenta albo oznaczyć je jako „pending mechanic".

## Runda 2 — weryfikacja (wypełnia reviewer)
- Naprawy zweryfikowane (wszystkie pozycje były P2 — advisory; brak P0/P1 do rozwiązania):
  - **P2-1 OK (backlog)** — i_c7/i_k4 nadal martwe, ale słusznie odłożone: konsumenci (golden oferty, efektywność QA-bota) są w future-scope, poza dozwolonym <10-linii P2 (klasa qaMultiplier z M6). Zgodne ze znaleziskiem + PLAN-ISSUE.
  - **P2-2 OK** — `performRewrite` (prestige.ts:200-204) iteruje `Object.keys(state.stats)` (snapshot — bezpieczne kasowanie w pętli) i usuwa wyłącznie prefiks `bugs.spawnedAt.`, po czym ustawia klucz boot; trwałe liczniki nietknięte. Test: `prestige.test.ts:74` ustawia `getBugSpawnedAtStatKey("p_llama_todo.1")=123`, `:104` asercjonuje `undefined` po rewrite. Helper `getBugSpawnedAtStatKey` wyeksportowany z debt.ts (centralizacja klucza).
  - **P2-3 OK** — `startGenerators` dodane do `RewritePreviewView` (render.ts:160) i `RewriteNodes` (:358), mapowane w main.ts przez `formatStartGenerators` (:678, :900-907; filtruje count>0, etykieta przez `t(generator.nameKey)` lub `ui.rewrite.startGeneratorsNone`), wyrenderowane jako wiersz forecast (render.ts:811) + `setText` (:1802). Klucze i18n obecne (en.json:320-321). Hard rule OK: formatowanie/lookup w main.ts (root kompozycji), render.ts tylko `text()/setText()` — bez game-math. Graceful fallback dla nieznanego id (bez crasha).
  - **P2-4 OK** — `tick` (main.ts:322) zapamiętuje `wasBooting` PRZED inkrementem playtimeS, a na końcu `markVisibleChanged(wasBooting && !isRewriteBooting(state))` (:332) — wykrycie zbocza true→false, baner znika na granicy boot nawet przy locRate=0. Brak alokacji w hot-path (dwa tanie wywołania czystej funkcji `isRewriteBooting`; `markVisibleChanged(false)` to no-op poza tickiem zbocza). Inwalidacja niezłamana.
  - **P2-5 OK (backlog)** — pełnoekranowa animacja boot (06 §6 „juice") słusznie odłożona (>10 linii P2, polish); systemowa część boot (REWRITE_BOOT_S, isRewriteBooting, 3 s) działa.
  - **P2-6 OK** — nowy test „blocks REWRITE below the first and repeat thresholds" (prestige.test.ts:142-156): pierwszy rewrite gain 4 < REWRITE_MIN_FIRST 5 → `{ok:false, reason:"threshold"}`, rewrites=0; kolejny — requirement = insightSinceExit 20 × 0.25 = 5, gain 24−20=4 < 5 → ok:false, rewrites bez zmian (asercja `calculateRewriteRequirement`=5). Plus asercja `isRewriteBooting(result.state)` po deserializacji (:192). Testy zachowania, nie implementacji.
  - **P2-7 OK** — `agentdb.rvf`, `agentdb.rvf.lock`, `x.verdict` usunięte z dysku; `.gitignore` zawiera `agentdb.rvf*` + `x.verdict`; brak pliku-śmiecia w `git status`.
- Nowe P0 wprowadzone poprawkami: brak. Sprawdzone: kasowanie stats po prefiksie nie usuwa trwałych liczników (brak utraty danych); render.ts bez game-math (display-only); zbocze boot edge-triggered (bez per-frame alloc, bez zepsutej inwalidacji); `npm run check` zielony (tsc + eslint + prettier + **65/65 vitest** + validate PASS 49 plików).
- Decyzja: DONE — wszystkie znaleziska były P2 (advisory, nie blokują); 4 poprawki kodu (P2-2/3/4/6) zweryfikowane jako poprawne, 2 słusznie w backlogu (P2-1/5), 1 higiena (P2-7), poprawki bez nowych P0. M8 spełnia AC.

# Review #12 — M11
Reviewer: Claude Opus 4.8 | Data: 2026-06-14
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 6 wymiarów: achievements-numbers, audio-bleeps, animations-reduced-motion, stats-shortcuts-ui, save-perf-hotpath, ac-coverage + weryfikacja adwersarialna + sweep, 17 agentów; reviewer własnoręcznie potwierdził crash audio + brak dev-panelu + ring-buffer sparkline):
  DoD: `npm run check` zielony (tsc + eslint + prettier + vitest + validate); brak runtime deps (dependencies={}).
  CO DZIAŁA (zweryfikowane): 50 achievementów == 09 §10 (kategorie/progi); ACHIEVEMENT_LOC_BONUS=0.01, mnożnik additive (1+count·0.01) foldowany do locRate (test: 2→1.02); 5 bleepów WebAudio in-house (AudioContext/Oscillator/Gain, BleepKind click/error/message/ship/unlock), bez biblioteki; sparkline = ograniczony ring-buffer (LOC_RATE_SAMPLE_CAPACITY=120, indeks % CAPACITY, próbki w state.stats) — brak wycieku; skróty klawiszowe (render.ts:762 keydown + handleShortcut, spacja/escape/ekrany); `@media (prefers-reduced-motion: reduce)` istnieje (layout.css:1446); achievementy/próbki/audio-settings w istniejących stats/settings → BRAK zmiany kształtu GameState, SAVE_VERSION=1 (spójne).
  GŁÓWNE LUKI: (P1) alokacja per-tick w tickAchievements (07 §9 zero-GC-churn); audio `new AudioContext()` bez guardu → wyjątek przez bus.emit wywraca pętlę na webview/starym Safari; brak pełnoekranowej animacji REWRITE (06 §6 „najważniejsza animacja"); brak dev-panelu perf → AC1 nieweryfikowalne. (P2) kontrast --fg-2, axe-core niewpięty, per-frame rebuild stats/achievements off-screen, gte vs gt, glitch Aktu 4-5. -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P1-1** | `src/systems/achievements.ts:91,120,160,164-174` — `tickAchievements` (wołane bezwarunkowo co tick, main.ts:485, 10 Hz) alokuje na hot-pathie dla każdego niezdobytego achievementu: `bigStatGte` (8 LoC + 6 $) i `zeroDebtWithLifetimeLoc` re-parsują stałą-string przez `Big.from(condition.value)` → `Big.fromString` (regex `.exec()` + new Big) KAŻDY tick; `generatorTotalGte` (×4) woła `Object.values(...)` (nowa tablica); `researchFullBranch` tworzy `new Map()` + `Array.from()` — dlaczego: 07 §9 „Mutujące operacje Big w tick (zero GC churn)" + AC1 budżety perf; progi to stałe kompilacyjne (data/achievements.ts), więc da się sparsować raz na module-load — ~15-20 krótkożyciowych alokacji/tick we wczesnej grze (na late-game save większość odhaczona → short-circuit, ale a_loc_1e35/a_money_1e30/a_research_branch nadal alokują) — fix: sprecomputuj progi Big do stałych modułowych i zcache'uj licznik agentów/gałęzi; pętla tick robi tylko porównania mutujące.
  - fix-note: Precompiled Big achievement thresholds and replaced per-tick Object.values/Map/Array.from scans with cached branch id arrays and loops. Covered by `tests/achievements-stats.test.ts` ("does not parse Big thresholds while ticking achievements").
- [x] **P1-2** | `src/ui/audio.ts:32` — `ensureContext()` woła `new AudioContext()` BEZ try/catch, bez fallbacku `webkitAudioContext`, bez guardu `typeof`; typ to `AudioContext | undefined`, a `play()` ma defensywne `if (audio === undefined) return` (:47), ale ta gałąź jest martwa — gdy globalny `AudioContext` nie istnieje, konstruktor RZUCA zamiast zwrócić undefined; `bus.emit` (bus.ts:39-41) NIE izoluje wyjątków listenerów (brak try/catch) — dlaczego: 08 M11 „WebAudio bleeps"; ścieżka aktywna domyślnie (state sound:true, volume 0.3); zdarzenie z ticku (`shipped` main.ts:441 / `story:message` :215 / `bug:spawned`) → listener audio → `new AudioContext()` rzuca → propaguje przez bus.emit POZA tick do pętli rAF → pętla się wywraca (nie tylko cisza) na starym iOS Safari (tylko webkitAudioContext), headless, webview (m.in. WKWebView desktopu M12) — fix: owiń `new AudioContext()` w try/catch z fallbackiem `window.AudioContext ?? window.webkitAudioContext` i zwróć undefined przy braku (graceful no-op, jak sugeruje typ).
  - fix-note: Added guarded AudioContext creation with try/catch and webkitAudioContext fallback; unavailable or blocked audio now no-ops. Covered by `tests/achievements-stats.test.ts` M11 audio tests.
- [x] **P1-3** | `src/ui/render.ts:1134-1135` + `src/ui/layout.css:563-573` — pełnoekranowa sekwencja REWRITE (06 §6: „pełnoekranowa sekwencja 2-3 s — linie repo spadają, `git init`, kursor mruga … Najważniejsza animacja w grze") NIE jest zaimplementowana: jedyny artefakt to NIE-pełnoekranowy `<p class="rewrite-boot">` w siatce forecast z jednym pulsującym stringiem `ui.rewrite.booting`="git init --new-life" (animacja `comms-pulse` = fade opacity); brak overlaya pełnoekranowego, spadających linii repo, sekwencji git-init, mrugającego kursora (brak takich keyframes w CSS) — dlaczego: 08 M11 „wszystkie animacje z 06 §6" (wymagane, ≠ opcjonalny data-rain z §7); deferowane z M8 jako M11 scope — fix: zaimplementuj pełnoekranową, pomijalną sekwencję boot (spadające linie + git init + mrugający kursor), wyłączaną przy prefers-reduced-motion.
  - fix-note: Added fullscreen skippable REWRITE boot overlay with falling repo lines, git init command, blinking cursor, and reduced-motion hiding. Covered by `tests/render.test.ts` M11 Rewrite rendering test.
- [x] **P1-4** | `src/core/loop.ts` + `src/main.ts` (boot) — instrument weryfikacji AC1 nie istnieje: brak dev-panelu perf (07 §11 „licznik perf: ms/tick, ms/frame, heap") — brak obsługi `?dev`, brak time-warp/grantu, brak pomiaru czasu klatki w pętli rAF (loop.ts render(alpha) nie mierzy nic; performance.now w render.ts:1764 to throttle terminala), src/dev/ ma tylko validate.ts; jedyny test perf (production.test.ts:70-83) mierzy samo tickProduction na syntetycznym stanie, nie pełny tick, nie przez dev-panel, nie na realnym late-game savie — dlaczego: 08 M11 AC1 „budżety perf z 07 §9 trzymają się na realnym late-game savie (dev panel)" + 10 §5 (budżety mierzone w dev-panelu na fixturze 60h) — budżet „render frame < 4 ms" jest całkowicie niemierzalny → AC1 claimed-but-unproven — fix: dodaj dev-panel z licznikiem ms/tick + ms/frame + heap (i fixturę/time-warp late-game), by AC1 dało się zweryfikować.
  - fix-note: Added loop timing instrumentation plus `?dev=1` perf panel with ms/tick, ms/frame, heap, and +1h/+8h offline-style time warp. Covered by `tests/loop.test.ts` instrumentation test and `tests/dev-perf-panel.test.ts`.

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/systems/achievements.ts:120` — achievement „debt=0" używa `state.lifetime.loc.gte(Big.from("1e9"))` (≥), a 09 §10 Specjalne mówi „debt=0 przy LoC>1e9" (ściśle większe); stała 1e9 poprawna, różni się tylko operator — dlaczego: 09 §10; praktycznie nieobserwowalne (lifetime.loc monotoniczny Big, nigdy nie ląduje dokładnie na 1e9) — fix: zmień `gte`→`gt` jeśli zależy na dokładności.
  - fix-note: Changed the zero-debt achievement check from gte to strict gt. Covered by `tests/achievements-stats.test.ts` strict 1e9 regression test.
- [x] **P2-2** | `src/ui/layout.css:176,1005` (`color: var(--fg-2)`) — informacyjny tekst w `--fg-2` (#5d6b80) jest poniżej 06 §8 „≥ 4.5:1": nagłówki kolumn `.agent-list__header` (0.72rem) na --bg-1 = 3.52:1; tagi kanałów `.comms-message__channel` na --bg-2 = 3.35:1 — dlaczego: 06 §8 + M11 „kontrast audit"; to NIE wyłączone kontrolki (brak wyjątku WCAG), normalny tekst → axe color-contrast (serious, nie critical, więc AC2 formalnie spełnione) — fix: użyj --fg-1 (#9fb0c7, 7.4-9:1) dla informacyjnych etykiet albo podnieś luminancję --fg-2.
  - fix-note: Switched `.agent-list__header` and `.comms-message__channel` from --fg-2 to --fg-1; small CSS-only fix under the P2 limit.
- [x] **P2-3** | `src/main.ts:572,597` — `createDevFloorView` przebudowuje cały model widoku co BRUDNĄ klatkę, w tym `createStatsView` (sparkline: 120× `log10()` + budowa path-stringa) i `createAchievementsView` (50 POJO z interpolacjami `t()`), niezależnie od aktywnego ekranu; viewDirty ustawiane rutynowo przez res:changed → rebuild stats/achievements liczony nawet gdy ich ekrany ukryte — dlaczego: 07 §9 „render odświeża tylko brudne", „listy re-renderowane tylko przy zmianie struktury"; DOM jest cache'owany + zapisy text-node strażone (w budżecie 4 ms), więc to alokacja/recompute JS, nie DOM — fix: bramkuj ciężkie sekcje stats/achievements do aktywnego ekranu.
  - fix-note: → backlog
- [x] **P2-4** | `package.json:19` + `vite.config.ts:5` — AC2 axe-core nie jest wpięte: axe-core + jsdom są w devDeps, ale NIGDY nie wołane (brak importu/`axe.run`/`toHaveNoViolations` w src/tests/tools), brak testu a11y, vitest `environment:"node"` (DOM nigdy nie instancjonowany); brak testu kontrast/reduced-motion/keyboard — dlaczego: 08 M11 AC2 „axe-core bez błędów krytycznych"; to gate manualny (axe skanuje żywy DOM), a instrument (axe-core) JEST zainstalowany → P2 (słaby/brakujący test automatyczny, nie brak instrumentu) — fix: dodaj test a11y (jsdom + axe.run na zamontowanym UI) albo udokumentuj manualny przebieg axe w QA.
  - fix-note: → backlog
- [x] **P2-5** | `src/ui/layout.css:883` (terminal-glitch) — animacja „rzadki glitch" Aktów 4-5 (06 §6: „chromatic shift na 100 ms, max raz/min, wyłączany w settings") nie jest wpięta; jedyny glitch w kodzie to kupowalny skin motywu Paradox `x_theme_glitch` (.terminal--theme-glitch, ciągły 7s infinite, gated zakupem) — inny feature — dlaczego: 06 §6 + 08 M11 „wszystkie animacje z 06 §6"; rzadki ambientowy efekt (mniejsza waga niż REWRITE) → P2 — fix: dodaj act-gated (story.act≥4) chromatic-shift ~100 ms, max raz/min, z przełącznikiem w settings i prefers-reduced-motion.
  - fix-note: → backlog

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
- **Token --fg-2 „muted/disabled" (06 §2) nie spełnia 06 §8 „≥4.5:1" na żadnym tle.** 06 §2 definiuje `--fg-2: #5d6b80` w grupie „/* tekst */" jako „muted/disabled" (a więc tekst), a 06 §8 wymaga ≥4.5:1 dla WSZYSTKICH par tekst/tło — tymczasem --fg-2 ma max 3.68:1 (na --bg-0), poniżej progu na każdej powierzchni. To źródło P2-2. Pogodzalne: WCAG zwalnia wyłączone kontrolki, a --fg-1 (#9fb0c7, 7.4-9:1) jest zgodnym tokenem „muted" tekstu → doprecyzuj w planie, że --fg-2 jest tylko dla disabled (zwolnione z WCAG), a de-emfazowany tekst używa --fg-1.

## Runda 2 — weryfikacja (wypełnia reviewer)
(`npm run check` zielony: 100/100 vitest, validate PASS 55 plików.)
- Naprawy zweryfikowane:
  - **P1-1 OK** — progi Big sprecompilowane RAZ na module-scope: `COMPILED_ACHIEVEMENTS = ACHIEVEMENTS.map(... compileCondition ...)` (achievements.ts:53-58); tick (64-88) porównuje przez precompiled `condition.value: Big` (gte/gt, bez Big.from/regex); getTotalGeneratorCount `for..in` (bez Object.values); researchFullBranch używa RESEARCH_BRANCH_IDS (bez new Map/Array.from). Test zlicza `Big.from`=0 podczas ticku. Zero GC churn (07 §9).
  - **P1-2 OK dla pierwotnego scenariusza — ALE poprawka wprowadziła NOWY P0 (niżej).** ensureContext (audio.ts:38-54): `window.AudioContext ?? webkitAudioContext`, guard `typeof window`, undefined gdy brak, `new Context()` w try/catch → graceful no-op; brak rzutu gdy AudioContext NIEOBECNY.
  - **P1-3 OK** — realna pełnoekranowa sekwencja: `.rewrite-boot-overlay` (position:fixed inset:0) ze spadającymi liniami repo (keyframe `rewrite-line-fall`), git-init console + mrugającym kursorem (keyframe `rewrite-cursor`), przycisk Skip; wyłączane przy prefers-reduced-motion; auto-dismiss na zboczu boot (main.ts:503) + skip → brak soft-locka; test render.test.ts.
  - **P1-4 OK** — perf dev-panel pokazuje ms/tick + ms/frame + heap z realnych pomiarów `measure()` (loop.ts:72/78), bramkowany `import.meta.env.DEV && ?dev=1`; w produkcji metrics=undefined → `measure()` short-circuit (loop.ts:94-97) → zero overheadu/UI w prod; warp 1h/8h dla late-game AC1. Testy loop.test.ts + dev-perf-panel.test.ts.
  - P2-1 OK (gt zamiast gte + test 1e9), P2-2 OK (informacyjny tekst na --fg-1 ≥4.5:1: layout.css:175/1117); P2-3/P2-4/P2-5 → backlog (świadomie, advisory P2 — nie blokują).
- Nowe P0 wprowadzone poprawkami:
  - **P0-NOWY** | `src/ui/audio.ts:79-90` — poprawka P1-2 zrefaktorowała `play()` z bezpośredniego (związanego) `window.requestAnimationFrame(() => …)` na ODŁĄCZONY uchwyt: `const requestFrame = getAudioWindow()?.requestAnimationFrame ?? (fallback); requestFrame(() => {oscillator.stop()…})`. W realnej przeglądarce (bundle = ES module / strict) wywołanie odłączonego `requestAnimationFrame` rzuca `TypeError: Illegal invocation` (brand-check wymaga `this===window`). Gdy AudioContext ISTNIEJE i dźwięk włączony (domyślnie sound:true) KAŻDY bleep — pierwszy klik PROMPT lub zdarzenie z ticku `shipped`/`story:message`/`bug:spawned` (main.ts:445/451/457) — trafia w to, a rzut propaguje przez `bus.emit` (bus.ts:39-41, nadal bez try/catch) POZA tick → pętla rAF się wywraca. Poprawka zamieniła „crash gdy AudioContext NIEOBECNY" na „crash gdy AudioContext OBECNY" (przypadek normalny). Testy zielone, bo środowisko node bierze synchroniczny fallback. — fix: wołaj rAF związany z window (`getAudioWindow()?.requestAnimationFrame?.bind(window)` albo `(cb) => win.requestAnimationFrame(cb)`), ewentualnie owiń planowanie cleanupu w try/catch.
    - fix-note: Bound `requestAnimationFrame` to the audio window before scheduling cleanup, so browser brand checks keep `this===window`. Covered by `tests/achievements-stats.test.ts` ("keeps browser requestAnimationFrame bound while scheduling cleanup").
- Runda 3 — weryfikacja nowego P0: **P0-NOWY OK** — `play()` (audio.ts:79-85) wiąże teraz rAF: `const audioWindow = getAudioWindow(); const requestFrame = audioWindow?.requestAnimationFrame?.bind(audioWindow) ?? (synchronous fallback);` → w realnej przeglądarce `this===window` (brak „Illegal invocation"), w headless synchroniczny fallback. Brak rzutu na ścieżce play() niezależnie od obecności AudioContext. Test regresyjny „keeps browser requestAnimationFrame bound while scheduling cleanup" (achievements-stats.test.ts, 11/11) + `npm run check` zielony (validate PASS 55 plików). Poprawka izolowana (jedna związana funkcja per bleep — pomijalne), bez kolejnych nowych P0.
- Decyzja: DONE — wszystkie 4 pierwotne P1 rozwiązane i zweryfikowane, P2-1/2 naprawione, P2-3/4/5 świadomie w backlogu, a P0 wprowadzony przez poprawkę P1-2 (odłączony `requestAnimationFrame`) naprawiony (związany rAF) i zweryfikowany testem + zielonym checkiem. Brak nierozwiązanych P0/P1. (PLAN-ISSUE --fg-2 pozostaje notatką dla człowieka, nieblokującą.)

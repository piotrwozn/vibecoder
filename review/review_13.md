# Review #13 — M12
Reviewer: Claude Opus 4.8 | Data: 2026-06-14
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 5 wymiarów: demo-gating-boundary, platform-desktop-web, tauri-config-ci-updater, save-portability-soak-qa, scope-hardrules + weryfikacja adwersarialna + sweep, 12 agentów; reviewer własnoręcznie potwierdził granicę demo, atomic-write, tauri.conf, release.yml):
  DoD: `npm run check` zielony (tsc + eslint + prettier + vitest + validate); `dependencies: {}` (BRAK runtime deps — @tauri-apps/cli to devDep).
  CO DZIAŁA (zweryfikowane): Demo gating == 07 §10 DOKŁADNIE — granica „do końca Aktu 1 + E2": E1 e_parrot_1/E2 e_muse wolne, E3-E10 (e_golem…e_omega) demoLocked; Akt 0/1 0× demoLocked, Akty 2-5 + echoes wszystkie demoLocked; generatory/hardware/projekty/upgrade'y E3+ flagowane; isDemoLocked czysty predykat (edition==="demo" && demoLocked), egzekwowany na buy + getVisible/getAvailable + tick (reason "demoLocked"); systemy IDENTYCZNE między edycjami (gating tylko na contencie); BRAK wycieku płatnego contentu (sweep: missedCount 0). Test m12-release.test.ts pokrywa demo-block E3 (era/agenci/hardware/projekty/upgrade'y/story) + full-allow + demo→full import. Platform desktop: Tauri przez `window.__TAURI__` (bez importu @tauri-apps/api → bez runtime dep); Rust lib.rs: write_synced(.tmp) → rotate_backups → fs::rename = ATOMIC + backupy (07 §10). tauri.conf: bundle „all", updater (pubkey+endpoints+passive), ikony. release.yml: matrix macOS/ubuntu/windows + tauri-action (signing keys + createUpdaterArtifacts + includeUpdaterJson) + build:demo → upload-pages-artifact + deploy-pages + sim sane 80h. Ekran „Get the full game" + i18n (ui.demo.fullGameTitle/Copy) + export sava zawsze dostępny (AC3). Save demo→desktop import: test (edition→full, zasoby zachowane).
  GŁÓWNE LUKI: (P1) brak vite `base` → absolutne /assets/ → blank na itch.io/Pages (AC2). (P2) brak ostrzeżenia trybu prywatnego, wersja 0.0.0 (footgun updatera AC4), brak ×100 warp dla soak, QA checklist bez pkt 7 (finał/3 endingi), test odporności save tylko ucięty JSON. -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P1-1** | `vite.config.ts:3-8` — brak ustawienia `base` w configu Vite (plik zawiera tylko blok `test` z vitest/config), więc build webowy emituje absolutne ścieżki assetów `/assets/...` (potwierdzone w `dist/index.html`: `src="/assets/index-*.js"`, `href="/assets/index-*.css"`); na itch.io (embed HTML w iframe na głębokiej ścieżce *.itch.zone) i na GitHub Pages project-page (`/<repo>/`) absolutne ścieżki dają 404 → biała strona → demo NIE startuje — dlaczego: 08 M12 AC2 „demo w przeglądarce" + 11 §1 (kanał itch.io: „zip na itch") + własny `deploy-pages` w release.yml:49-58; build+deploy wg planu produkuje blank na nazwanym kanale (granica P0/P1 — blokuje merge; AC2 spełnialne tylko z roota domeny) — fix: ustaw `base: "./"` w vite.config (lub `--base ./` w buildzie demo) dla hostingu pod-ścieżkowego.
  - fix-note: Added `base: "./"` in `vite.config.ts`; regression coverage in `tests/m12-release.test.ts` asserts the relative base, and `npm run build:demo` now emits `./assets/...`.

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/platform/web.ts:9-15` — `web.ts` robi localStorage get/set BEZ try/catch i bez wykrycia/ostrzeżenia trybu prywatnego, którego plan wprost wymaga (07 §10: „localStorage (+ ostrzeżenie o trybie prywatnym)"); w Safari Private / Firefox z zablokowanym storage `setItem` rzuca QuotaExceededError → saveGameState (save.ts:88-101) cicho zwraca false → autosave w kółko zawodzi bez żadnego toasta, gracz traci postęp bez informacji — dlaczego: 07 §10; brak crasha (load ma fallback do defaults), edge-case → P2 — fix: wykryj niedostępny/zablokowany localStorage przy starcie i pokaż jednorazowe ostrzeżenie (i18n) o trybie prywatnym.
  - fix-note: → backlog
- [x] **P2-2** | `src-tauri/tauri.conf.json:4` (+ `Cargo.toml:3`, `package.json:4`) — wersja aplikacji to placeholder `0.0.0` we wszystkich 3 plikach na milestonie wydania (M12); `release.yml:97` `--config` wstrzykuje tylko createUpdaterArtifacts + updater.pubkey, NIE wersję, a tauri-action bierze wersję z tauri.conf (nie z gita taga) → każdy tagowany build osadza 0.0.0 i latest.json raportuje 0.0.0 → updater porównuje 0.0.0 vs 0.0.0 (brak „>") → nigdy nie oferuje aktualizacji (footgun AC4) — dlaczego: 11 §2 (SemVer, „1.0.0 = premiera", git tag per release) + 08 M12 AC4; infra updatera JEST poprawna (pubkey/endpoints/signing), brak tylko bumpa wersji → P2 — fix: bumpnij wersję w 3 plikach przy cięciu release'u (albo wstrzykuj wersję z taga w release.yml `--config`).
  - fix-note: Bumped release metadata to `1.0.0` in `package.json`, `package-lock.json`, `src-tauri/Cargo.toml`, and `src-tauri/tauri.conf.json`.
- [x] **P2-3** | `src/main.ts:565-573` + `src/dev/perf-panel.ts:36-41` — instrument soak daje tylko dyskretne skoki offline, nie ×100 ciągły time-warp z 10 §5: `runDevTimeWarp` ustawia `meta.lastSeen -= N h` i woła `applyOfflineProgress` RAZ (offline.ts — closed-form, capowane do offline cap), a panel ma tylko przyciski +1h/+8h; closed-form skok NIE tika żywej pętli, więc „heap płaski (zrzut co godzinę)" i „FPS stabilny" (sedno soaku) nie da się zmierzyć; `release/m12-qa.md:6` każe „run continuously for 8h at x100 time warp", co jest niewykonalne obecnym narzędziem — dlaczego: 10 §5 (soak = 8h ×100, heap płaski/FPS); scenariusz RĘCZNY przedwydaniowy, nie jeden z 4 AC → P2 — fix: dodaj ciągły mnożnik prędkości (×10/×100, 07 §11) który napędza żywą pętlę tick/render, by soak mierzył heap/FPS w czasie.
  - fix-note: → backlog
- [x] **P2-4** | `release/m12-qa.md:12-23` — repo-wa checklista QA (pełna lista wg 10 §6) pomija punkt 7: „Finał + wszystkie 3 endingi + wejście w ITERATION (save przed finałem w fixtures)"; brak wiersza o finale/3 endingach/ITERATION i brak fixtury save-przed-finałem (`tests/fixtures/saves/` ma tylko świeży v1.json) — dlaczego: 10 §6 pkt 7 + 08 M12 „QA checklist (10 §6)"; ścieżka finału/3 endingów nie ma pokrycia w checklist przedwydaniowej → regresja endingu/ITERATION może przejść niezauważona → P2 (niekompletna ręczna checklista) — fix: dodaj wiersz QA dla finału/3 endingów/ITERATION + dorzuć fixturę save-przed-finałem.
  - fix-note: → backlog
- [x] **P2-5** | `tests/save.test.ts:51-64` — test odporności save pokrywa tylko ucięty JSON (`"{broken"`), a 10 §4 wprost wymaga trzech typów uszkodzenia: „ucięty JSON, brakujące pola, NaN"; kod naprawczy (repairNumber via Number.isFinite save.ts:434-439; repairBig try/catch :477-484; Big.fromString odrzuca „NaN") OBSŁUGUJE missing-fields i NaN, ale brak testu regresyjnego → osłabienie naprawy w przyszłości przejdzie zielone CI — dlaczego: 10 §4; kod poprawny, więc luka pokrycia → P2 — fix: dodaj przypadki testowe dla save z brakującymi sekcjami i z `res.loc="NaN"` (asercja repaired=true, brak crasha/NaN).
  - fix-note: → backlog

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
_(brak)_

## Runda 2 — weryfikacja (wypełnia reviewer)
(`npm run check` zielony: 108/108 vitest, validate PASS 56 plików, `vibecoder@1.0.0`.)
- Naprawy zweryfikowane:
  - **P1-1 OK** — `vite.config.ts:4` ma teraz `base: "./"`; `npm run build:demo` emituje ŚCIEŻKI WZGLĘDNE: `dist/index.html` → `src="./assets/index-*.js"`, `href="./assets/index-*.css"` (potwierdzone własnoręcznym buildem). Na itch.io (embed pod-ścieżkowy) i GitHub Pages project-page względne `./assets/` rozwiązują się poprawnie → demo startuje (AC2). `base:"./"` jest też bezpieczny dla Tauri (asset protocol z indexem w roocie) i hostingu z roota domeny.
  - **P2-2 OK** — wersja podbita do `1.0.0` spójnie w `package.json:4`, `src-tauri/tauri.conf.json:4`, `src-tauri/Cargo.toml:3` (11 §2 „1.0.0 = premiera"); usuwa footgun updatera (latest.json zaraportuje 1.0.0 > zainstalowanej testowej). Bump przy kolejnych release'ach pozostaje ręczny (zgodnie z planem).
  - P2-1 (ostrzeżenie trybu prywatnego), P2-3 (×100 warp dla soak), P2-4 (QA checklist pkt 7 + fixtura przed-finałowa), P2-5 (test odporności save: missing-fields/NaN) → backlog (świadomie, advisory P2 — nie blokują; kod naprawczy save już poprawny, soak/QA to ręczne narzędzia przedwydaniowe).
- Nowe P0 wprowadzone poprawkami: brak. Sprawdzone: `base:"./"` nie psuje żadnego targetu (web-root / sub-path / Tauri) — same ścieżki względne; bump wersji spójny we wszystkich plikach (brak rozjazdu). 108/108 testów + validate PASS.
- Decyzja: DONE — jedyny blocker (P1-1 vite base) rozwiązany i zweryfikowany realnym buildem (`./assets/`), P2-2 naprawione (wersja 1.0.0 spójna), pozostałe P2 świadomie w backlogu, poprawki bez nowych P0. M12 (ostatni milestone) spełnia AC; demo gating, platforma desktop (atomic+backupy), matrix CI + updater i przenośność save'a zweryfikowane w rundzie 1.

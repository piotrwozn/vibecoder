# Review #14 — M13
Reviewer: Claude Opus 4.8 | Data: 2026-06-15
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 5 wymiarów: save-migration-ui, window-manager, boot-zoom-reduced-motion, sticky-notes-tokens, hardrules-perf-scope + weryfikacja adwersarialna + sweep, 14 agentów; reviewer własnoręcznie potwierdził migrację v1→v2, oba P1, brak scene-guardu, createDevFloorView):
  DoD: `npm run check` zielony (vitest + validate). Zakres M13 = przebudowa top-level UI na „desktop OS" (boot+pulpit+okna+karteczki), NIE rusza ekonomii (02/03).
  CO DZIAŁA (zweryfikowane): SAVE — SAVE_VERSION bumpnięte 1→2, migracja v1→v2 (migrations.ts:20-29) dodaje domyślne `ui` (`createDefaultUiState("desktop", true)`) starym savom BEZ utraty danych (`...raw`), fixtury v1.json (bez ui) + v2.json (z ui), test save.test.ts:85-100 migruje v1→desktop + headless boot 60 s (repaired=true, v=2), v2 round-trip repaired=false — AC „układ okien przeżywa reload" spełnione. Window manager (ui/wm/window-manager.ts) = CZYSTE funkcje geometrii (bez DOM): openWindow jedno-okno-per-app + focus z-order, move/resize klamrowane do granic + min-size, maximize/restore, resetWindowLayout — pokryte m13-window-manager.test.ts. GameState.ui {bootSeen, scene, windows: Record<AppId,WindowState>} (9 apek). Brak runtime deps (dependencies {}); font PatrickHand vendorowany (.ttf, bez @font-face do CDN).
  GŁÓWNE LUKI: (P1) skróty klawiszowe bez scene-guardu → na ekranie BOOT Spacja mutuje ekonomię, 1-7 omija zoom; (P1) zamknięte/zminimalizowane okna nadal liczą pełny view-model co brudną klatkę (AC „okno zamknięte nic nie liczy"). (P2) słabe testy save (geometria/maximized/restore), tooltip Money/s bez rozbicia mnożników, skrót Achievements↔Settings, token --sp-5 niezdefiniowany, ikony bez stanu locked, brak testu sceny boot. -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P1-1** | `src/ui/render.ts:848-849` + `:2270-2317` — `handleShortcut` jest podpięty globalnie do `window` keydown BEZ żadnego sprawdzenia sceny (sygnatura `(event, desktop, terminal, actions)` — brak `view.ui.scene`; jedyne guardy to ctrl+Tab, klawisze modyfikujące, editable target), więc skróty „desktopowe" są aktywne na scenie BOOT — dlaczego: 06 §11 (BOOT to osobna scena wchodzona przez START, skippable tylko klik/Esc) + 06 §8 (Space/1-7 to skróty ekranów gry); na świeżym savie (scene="boot", state.ts:202): (a) Spacja (body sfokusowane, brak autofocusu przycisku) → `runPromptClick` → `performPromptClick` mutuje ekonomię (`res.loc`/`lifetime.loc`/`locSinceExit`, prompt.ts:10-34) PRZED startem gry + gra dźwięk; (b) klawisz 1-7 → `actions.openApp` (main.ts) ustawia `scene="desktop"` i otwiera okno, omijając sekwencję START→zoom→desktop (i bleep boot) — fix: bramkuj `handleShortcut` na `scene === "desktop"` (przekaż aktualną scenę i wczesny return na boot).
  - fix-note: `handleShortcut` now reads the current scene from `DesktopNodes` and returns early unless `scene === "desktop"`; covered by `tests/render.test.ts` ("ignores game shortcuts while the boot scene is active").
- [x] **P1-2** | `src/main.ts:674-716` (createDevFloorView) — `updateVisibleView` woła `createDevFloorView` co BRUDNĄ klatkę i bezwarunkowo buduje view-model WSZYSTKICH 9 apek (`createRewriteView`/`createResearchView`/`createProjectsView`/`createStatsView`/`createAchievementsView`/`createUpgradeViews`/`createHardwareViews`/…) bez sprawdzenia stanu okna (open/minimized) — `render.ts` bramkuje tylko ZAPISY DOM (`isAppVisible`), ale NIE liczenie view-modelu; podczas normalnej gry res:changed „loc" co tick → viewDirty → rebuild wszystkich apek (w tym `createRewritePreview`/`createIterationPreview`/`createExitPreview` — matematyka prestiżu + `getInsightTree().map`/`getResearchTree().map`/`getAchievementStates().map`) dla ZAMKNIĘTYCH okien — dlaczego: AC M13 „budżet perf 07 §9 trzyma się (okno zamknięte/min nic nie liczy)" + 06 §12 „okno zamknięte/zminimalizowane = apka nic nie liczy (dirty-flag, nie render w tickach)"; ograniczone (mnożniki cache'owane w DerivedCache, brak alokacji DOM) → P1, nie P0 — fix: bramkuj budowę view-modelu per-apka na `isWindowVisible(state.ui.windows[appId])` (licz tylko otwarte, nie-zminimalizowane okna).
  - fix-note: `createDevFloorView` now does one full initial build, then gates per-app view-model creation with `shouldBuildAppView` and reuses prior closed/minimized app views; covered by `tests/m13-window-manager.test.ts` ("builds app view-models only for visible windows after initial mount").

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `tests/fixtures/saves/v1.json` + `v2.json` + `tests/save.test.ts:85-112` — testy save są słabe wobec wymagań M13: (a) v1.json ma wszystkie zasoby zero/empty, więc test migracji v1→v2 NIE demonstruje „bez utraty danych" (regresja usuwająca `...raw` przeszłaby — repairBig i tak zeruje); (b) v2.json ma wszystkie okna maximized:false/minimized:false i BRAK pola `restore`, a round-trip asercjonuje tylko `windows.agents.open===true` → zachowanie geometrii (x/y/w/h/z), maximized=true i `restore` nie jest pokryte (ścieżka repairWindowFrame present-restore zero coverage) — dlaczego: 08 M13 AC „układ okien przeżywa reload (test save/migracja)" + 06 §12 „persist pozycja/rozmiar/min/max"; kod naprawczy POPRAWNY (save.ts:836-891), luka pokrycia → P2 — fix: v1 fixture z niezerowym loc/money + asercja przetrwania; v2 fixture z oknem maximized:true + restore + asercja geometrii po round-tripie.
  - fix-note: → backlog (fixture expansion and additional save assertions exceed the <10 line advisory budget).
- [x] **P2-2** | `src/main.ts:1468-1472` (+ `en.json` ui.tooltip.moneyRate) — tooltip „Money/s" na karteczce NIE jest rozbiciem mnożników: `createMoneyRateTooltip` zwraca tylko total + statyczny tekst („Total {total}; project revenue after multipliers"), gdy „LoC/s" (createLocRateTooltip) itemizuje era/research/insight/debt/prestige/upgrades/achievements — dlaczego: 06 §13 „Tooltip mnożników (§5) podpięty pod LoC/s I Money/s" + §5; czynniki są dostępne (getProjectIncomeRate: hype·prestige·revenueMultiplier) → P2 (total pokazany, brak itemizacji) — fix: rozbij tooltip Money/s na hype/prestige/revenueMultiplier (jak LoC/s).
  - fix-note: `createMoneyRateTooltip` now itemizes hype, prestige, and revenue multiplier via i18n text; no separate regression test added because this was advisory P2 text plumbing under the <10 line budget.
- [x] **P2-3** | `src/ui/render.ts:780` vs `:2344` — niespójność skrótu: ikona pulpitu Achievements reklamuje skrót „7" (screenLinks shortcut "7" → tooltip „Achievements (7)" via ui.desktop.iconTitle), ale `getShortcutApp("7")` zwraca `settings` (brak case'a achievements) → Achievements nie ma działającego skrótu, a „7" otwiera Settings (którego ikona nie reklamuje skrótu) — dlaczego: 06 §8/§12 (mapowanie 1-7 pod nowy zestaw apek); obie apki klikalne ikoną, klawisz 7 otwiera ważne okno → P2 — fix: zsynchronizuj getShortcutApp z screenLinks (achievements↔7 albo zdejmij skrót z ikony Achievements i dodaj do Settings).
  - fix-note: Moved shortcut "7" from Achievements to Settings in `screenLinks` so the advertised desktop shortcut matches `getShortcutApp`.
- [x] **P2-4** | `src/ui/layout.css:1837` (+ :526/:801/:1585/:1619) — używany token `--sp-5` nie jest nigdzie zdefiniowany (theme.css definiuje --sp-1/-2/-3/-4/-6, zgodnie z 06 §2), więc `padding: var(--sp-5)` na ekranie monitora BOOT (`.boot-scene__screen`) jest invalid-at-computed-value → kolapsuje do 0; tytuł/przyciski boot przylegają do krawędzi monitora (i 4 inne miejsca degradują) — dlaczego: 06 §2 (zestaw tokenów) + §11 (layout boot) — fix: zdefiniuj `--sp-5` w theme.css (albo zamień użycia na istniejący token).
  - fix-note: Added `--sp-5: 20px` to `src/ui/theme.css`.
- [x] **P2-5** | `src/ui/render.ts:1078-1105` (createDesktopIcons) — ikony apek pulpitu są budowane bezwarunkowo ze screenLinks, bez stanu locked/wyszarzonego; 06 §12 wymaga: „Apka zablokowana = wyszarzona / „???" do spełnienia unlocku (spójnie z systemem widoczności 02 §8)"; `getUnlockVisibility` (systems/unlocks.ts) istnieje, ale nie jest konsultowane dla apek → świeży gracz widzi Rewrite/Research jako normalne, otwieralne ikony przed odblokowaniem — dlaczego: 06 §12 + 02 §8; systemy nadal bramkują akcje (zakupy), więc to luka afordancji UI → P2 — fix: wyszarzaj/„???" ikonę apki dopóki jej unlock nie jest spełniony (konsultuj getUnlockVisibility).
  - fix-note: → backlog (requires passing unlock visibility into the desktop icon view model/render path and exceeds the <10 line advisory budget).
- [x] **P2-6** | `tests/` (brak testu sceny boot) — żaden test nie pokrywa AC boot M13: decyzja reduced-motion-vs-zoom (render.ts:978-980) i przejście skippable klik/Esc są nietestowane; render.test.ts montuje od razu scenę „desktop" — dlaczego: 08 M13 AC „boot→zoom→desktop działa i jest skippable; prefers-reduced-motion = bez zoomu"; regresja usuwająca guard reduced-motion albo skip przeszłaby zielone CI → P2 (słaby/brakujący test) — fix: dodaj test sceny boot (fade przy reduced-motion, skip klik/Esc → desktop, scene/bootSeen).
  - fix-note: → backlog (full boot transition/reduced-motion coverage exceeds the <10 line advisory budget; P1 boot shortcut regression is covered in `tests/render.test.ts`).

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
_(brak)_

## Runda 2 — weryfikacja (wypełnia reviewer)
(`npm run check` zielony: 115/115 vitest, validate PASS 58 plików.)
- Naprawy zweryfikowane:
  - **P1-1 OK** — `handleShortcut` (render.ts:2279) ma teraz guard `if (event.defaultPrevented || desktop.currentScene !== "desktop") return;` na początku → Spacja i 1-7 są bezczynne na scenie BOOT (brak mutacji ekonomii / brak omijania zoomu); na desktopie działają normalnie. Test „ignores game shortcuts while the boot scene is active" (render.test.ts).
  - **P1-2 OK** — `createDevFloorView` (main.ts:676-716) bramkuje budowę view-modelu per-apka przez `shouldBuildAppView(state.ui.windows, appId)`: okno OTWARTE → `createXView()` świeże co klatkę (brak staleness dla widocznych); okno zamknięte/zminimalizowane → reuse `previous` (zero liczenia). Otwarcie okna przełącza gate na true → świeży rebuild (brak nieaktualnych danych). Initial mount (includeClosedApps) populuje `lastDevFloorView`, więc steady-state zamknięte apki nic nie liczą. Test „builds app view-models only for visible windows after initial mount" (m13-window-manager.test.ts). Spełnia AC „okno zamknięte/min nic nie liczy".
  - P2-2 OK (createMoneyRateTooltip itemizuje hype/prestige/revenue, main.ts:1485 + i18n); P2-3 OK (skrót „7" przeniesiony na Settings, render.ts:791 → zgodny z getShortcutApp("7")=settings; Achievements bez fałszywej reklamy skrótu); P2-4 OK (`--sp-5: 20px` w theme.css:52). P2-1 (testy save geometria/restore), P2-5 (ikony locked/„???"), P2-6 (test sceny boot) → backlog (świadomie, advisory P2 — nie blokują).
- Nowe P0 wprowadzone poprawkami: brak. Sprawdzone: guard sceny nie psuje skrótów desktopu (currentScene==="desktop" → działają); gating view-modelu nie wprowadza staleness dla otwartych okien (zawsze rebuild) ani nie gubi danych po otwarciu (gate→true→świeże); przeniesienie skrótu 7 spójne (jedna apka na klawisz). 115/115 testów + validate PASS, brak runtime deps.
- Decyzja: DONE — oba P1 (scene-guard skrótów na boot; zamknięte okna nic nie liczą) rozwiązane i zweryfikowane (kod + testy regresyjne + 115/115), P2-2/3/4 naprawione, P2-1/5/6 świadomie w backlogu, poprawki bez nowych P0. M13 (powłoka desktop) spełnia AC; SAVE_VERSION 1→2 + migracja + wm zweryfikowane w rundzie 1.

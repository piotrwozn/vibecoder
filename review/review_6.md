# Review #6 — M5
Reviewer: Claude Opus 4.8 | Data: 2026-06-13
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 5 wymiarów + weryfikacja adwersarialna + sweep):
  DoD: `npm run check` zielony (tsc + eslint + prettier + vitest + validate PASS).
  Liczby/wzory == 03 §6: dDebt=locRate×DEBT_FACTOR(0.01)×(1-quality=0)×dt; debtEff=1/(1+debt/D0)^0.35 (DEBT_EFF_EXP); D0=1000×eraMult^2 (eraMult=1.5^(era-1)); P(bug)=min(0.25, debtRatio/20); Refactor debt×0.4. Wszystkie potwierdzone.
  rng.ts = poprawny mulberry32 (07 §8); determinizm bugów działa (seedowany, twin-fixture w debt.test.ts).
  AC: (1) debtEff JEST wpięty w produkcję (production.ts:64-71, globalMultiplier=era×debt×prestige) i obniża locRate — ALE tylko przy recompute strukturalnym, NIE w czasie rzeczywistym → patrz P0-1. (2) bug pojawia się + fix działa (debt.test.ts), bug obniża przychód przez bugPenalty=0.4 (projects.ts:110). (3) ten sam seed = te same bugi (twin-fixture) — test istnieje, ale słaby (P2-4).
  unlocks.ts checkCondition pokrywa wszystkie pola 07 §5 (all/any, era, money/loc via Big, prestiż, ship, debtRatio, seen, flag, time, iteration). Hard rules: brak zależności runtime; data/ czyste; systems/ bez DOM; Big dla debt/loc/money. GameState bez zmiany kształtu (bugs[]/rngSeed istniały od M2) → SAVE_VERSION bez zmian (OK), M4-repair obsługuje bugs/rngSeed. Zakres: debt/unlocks/rng/terminal — bez scope creep. -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P0-1** | `src/main.ts:78-82` — zmiany długu NIE unieważniają DerivedCache, więc `debtEff`/`locRate`/tooltip są NIEAKTUALNE — dług NIE dusi produkcji w czasie rzeczywistym (sprzeczne z AC „debt realnie dusi produkcję wg wzoru (tooltip pokazuje debtEff)”). `accrueDebt` (`debt.ts:78-80`) i Refactor (`projects.ts:160-162`) emitują tylko `res:changed`/`production:changed` → `markResourceChanged` (tylko VIEW), a `recomputeDerivedCache` (gdzie liczony jest `multipliers.debt` i `locRate`, `production.ts:64-66`) odpala się w `render` wyłącznie przy `dirty.cache`, ustawianym tylko przez `markStructuralChanged` = `bought/bug:fixed/bug:spawned/unlock/era:changed/prestige` (`main.ts:84-95`) — dlaczego: w pustym/zabugowanym portfolio podczas idle ŻADEN event strukturalny nie pada → `cache.locRate` zamrożony → dług rośnie, ale produkcja mintowana po starej (niedławionej) stawce, a Refactor (×0.4) nie podnosi produkcji aż do następnego zakupu; tooltip `multipliers.debt` (`main.ts:492`) też stary — fix: licz debtEff „na żywo” w tickProduction (cache trzyma locRate BEZ debtu, mnóż przez aktualny debtEff w ticku) ALBO przelicz cache przy każdej zmianie długu; nie cache'uj debtEff tylko na zdarzeniach strukturalnych.
  - fix-note: Added `markResourceEvent` so `res:changed` for `debt` marks DerivedCache dirty, and wired `main.ts` to use it; covered by `tests/view-invalidation.test.ts` (`invalidates DerivedCache when debt changes`).

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/systems/unlocks.ts:26` — `checkCondition`/`getUnlockVisibility` to kod-sierota: zero wywołań poza `tests/unlocks.test.ts`; widoczność „???”/odblokowane generatorów idzie OSOBNĄ ścieżką (`isGeneratorUnlocked` era+previousId → `entry.unlocked` → `t("ui.unlock.hidden")`), a evaluator §5 nie jest wpięty w żadne bramkowanie — dlaczego: 07 §131 czyni `checkCondition` wspólnym evaluatorem dla unlocks+story, więc to prawdopodobnie celowe staging do M7 (story) — fix: potwierdź wpięcie evaluatora w M6/M7 albo usuń do czasu użycia (nieblokujące, evaluator istnieje i jest przetestowany zgodnie z deliverable M5).
  - fix-note: → backlog (wpięcie Condition DSL w story/unlock content jest poza M5 review-fix zakresem i nie mieści się w <10 liniach).
- [x] **P2-2** | `src/systems/unlocks.ts:42` — `Big.from(condition.moneyGte)` / `Big.from(condition.locLifetimeGte)` bez try/catch i bez walidacji; `Big.fromString` (`bignum.ts:63`) RZUCA „Invalid Big string” na łańcuchach niepasujących do regex (np. `"1,000"`, `"$1e3"`, `""`), a `validate.ts` nie sprawdza formatu pól Condition — dlaczego: AGENTS.md „validate input at boundaries”; 07 §7 „save nigdy nie odrzucany” — latentne (brak danych Condition i evaluator niewpięty), ryzyko aktywuje się przy contentcie M6/M7 — fix: waliduj format `moneyGte`/`locLifetimeGte` w `validate.ts` lub guard `Big.from` z fallbackiem „warunek niespełniony”.
  - fix-note: → backlog (proper boundary validation plus tests spans future Condition content and is >10 lines).
- [x] **P2-3** | `src/systems/unlocks.ts:82` — `timeInActMinGte` porównuje `state.meta.playtimeS` (całkowity czas gry), a nie czasu w bieżącym AKCIE — dlaczego: nazwa pola („InAct”, 07 §5) implikuje czas od wejścia w akt; latentne (brak systemu aktów do M7) — fix: w M7 dodać znacznik wejścia w akt i liczyć różnicę.
  - fix-note: → backlog (requires an act-entry timestamp/state shape from M7, so it is outside the <10-line P2 budget).
- [x] **P2-4** | `tests/debt.test.ts:33-66` — test determinizmu (AC „ten sam seed = te same bugi”) jest słaby: fixture ma DOKŁADNIE 1 kandydata i przesuwa playtimeS o 1×BUG_CHECK_INTERVAL, więc `nextRandomIndex(seed,1)` trywialnie zwraca index 0 (rng.ts:28), pada jeden `trySpawnBug`; nie weryfikuje KTÓRY produkt jest bugowany ani przesuwania strumienia RNG przez wiele spawnów — dlaczego: P2 „weak test” — fix: dodaj przypadek z ≥2 produktami i ≥2 checkami, porównując całe sekwencje bugów dwóch bliźniaczych stanów.
  - fix-note: → backlog (strong multi-candidate RNG fixture is a test expansion >10 lines; existing P0/P1 coverage unchanged).
- [x] **P2-5** | `tests/projects.test.ts:40-43` — kara przychodu za buga (`BUG_PENALTY=0.4`, `projects.ts:110`) nie jest asercjonowana przez żaden test; pokryta jest tylko ścieżka `bugged:false` — dlaczego: P2 „weak test” (konsekwencja gameplayowa buga niezweryfikowana) — fix: dodaj test: produkt `bugged:true` daje `revenue×0.4` w `getProjectIncomeRate`.
  - fix-note: Added an assertion in `tests/projects.test.ts` that bugged product income is multiplied by `C.BUG_PENALTY`.
- [x] **P2-6** | `src/main.ts:271` — etykieta flow-metera pokazuje „0%” przez całe okno flow: po napełnieniu paska `performPromptClick` (`prompt.ts:24-26`) zeruje `flow.meter=0` i ustawia `activeUntil`, więc `flowMeter:` = `Math.floor(0×100)%` = „0%”, podczas gdy pasek (`flowProgress: isFlowActive ? 1 : meter`) jest pełny i terminal świeci `--flow` — dlaczego: niespójność wizualna PROMPT flow-metera (06 §6), AC nie precyzuje tekstu — fix: pokaż „100%”/etykietę FLOW gdy `isFlowActive`.
  - fix-note: Updated `createDevFloorView` to show `100%` while flow is active and reuse that same active state for the bar.
- [x] **P2-7** | `src/ui/render.ts:689-695` — agregacja throttle terminala nadpisuje OSTATNI prawdziwy wiersz commita: gdy `windowCount>=10`, `setText(lastRow.text, "+{count} commits")` zjada 10. realny commit (`lastRow` wskazuje ostatni realny wiersz), zaniża licznik o 1 i zostawia nieaktualny tekst „+N commits” po resecie okna (do pełnego cyklu ringa 200) — dlaczego: 06 §6 „nadmiar agregowany” — agregacja działa, ale niszczy zatwierdzoną linię i błędnie liczy; trigger: spam PROMPT (Space autorepeat) >10/s — fix: użyj dedykowanego slotu agregacji zamiast nadpisywać `lastRow`, licz wszystkie pominięte.
  - fix-note: → backlog (dedicated aggregation slot and lifecycle cleanup is >10 lines, so deferred under the P2 rule).
- [x] **P2-8** | `{` (root repo) — pusty (0 B) plik-śmieć z pomyłki powłoki w drzewie roboczym; przy `git add .` trafi do commitu M5 — dlaczego: higiena zakresu (jak wcześniejsze `308'`/`value.append(...)` z M2) — fix: usuń plik przed commitem.
  - fix-note: Removed the exact empty root file `{`; no test needed for workspace hygiene.

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
<!-- pozycje, z którymi Codex się nie zgadza: id znaleziska + uzasadnienie z cytatem z planu -->
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
_(brak)_

## Runda 2 — weryfikacja (wypełnia reviewer)
- Naprawy zweryfikowane:
  - **P0-1 OK** — nowy `markResourceEvent` (`view-invalidation.ts:47-56`) routuje `res:changed` SELEKTYWNIE: `debt` → `markStructuralChanged` (cache+view), reszta → `markResourceChanged` (tylko view); `main.ts:78-80` używa go z kluczem zasobu. Przyrost długu i Refactor (oba emitują `res:changed:debt`, debt.ts:79 / projects.ts:162) unieważniają teraz cache → `recomputeDerivedCache` aktualizuje `multipliers.debt`/`locRate`/tooltip w czasie rzeczywistym; pusty/zabugowany portfolio idle też (dług rośnie → res:changed:debt → recompute). `loc`/`money` (co tick) nadal tylko view — brak szerszej regresji. Test `tests/view-invalidation.test.ts:15-22` (`money`→cache:false, `debt`→cache:true).
  - P2-5 OK (`tests/projects.test.ts:45-48` asercja dochodu zabugowanego = `revenue×BUG_PENALTY×hype`); P2-6 OK (`main.ts:274` `flowMeter` = „100%” gdy flow aktywny); P2-8 OK (plik `{` usunięty).
  - P2-1, P2-2, P2-3, P2-4, P2-7 → backlog (świadomie, P2, >10 linii / zależne od contentu M6-M7 — dozwolone wg AGENTS.md).
- Nowe P0 wprowadzone poprawkami: brak. (Obserwacja nieblokująca, nie-P0: ponieważ dług narasta co tick, `res:changed:debt` wyzwala teraz pełny `recomputeDerivedCache` co KLATKĘ podczas produkcji — wraca wzorzec „recompute co klatkę” z 07 §9; akceptowalne przy 3 generatorach E1 w M5, ale warto w M9 liczyć `debtEff` na żywo w `tickProduction` zamiast pełnego recompute, zanim liczba generatorów wzrośnie.)
- Decyzja: DONE — jedyny blocker (P0-1) rozwiązany i przetestowany, fix selektywny (bez regresji loc/money), poprawki nie wprowadziły nowych P0; P2 naprawione (5/6/8) lub w backlogu (1/2/3/4/7); `npm run check` zielony.

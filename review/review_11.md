# Review #11 — M10
Reviewer: Claude Opus 4.8 | Data: 2026-06-14
Status: DONE   <!-- OPEN → FIXED (Codex) → DONE / ESCALATE (reviewer, runda 2) -->
VERDICT: CHANGES REQUIRED

<!-- Kontekst (zweryfikowane przez reviewera; przegląd wieloagentowy 6 wymiarów: iteration-paradox-numbers, iteration-reset, paradox-shop-echoes, auto-rewrite-rules-cosmetics-ui, save-migration-hotpath, sim-ac-coverage + weryfikacja adwersarialna + sweep, 16 agentów; reviewer własnoręcznie potwierdził oba P0 + uruchomił sim):
  DoD: `npm run check` zielony (tsc + eslint + prettier + vitest + validate); `npm run sim -- --endless-iterations 5` → iterations=5, paradox=31, max_e=30, EXIT=0.
  CO DZIAŁA (zweryfikowane): formuły M10 zaimplementowane I STOSOWANE — softcap `threshold×(rate/threshold)^0.9` (iteration.ts:21-30, SOFTCAP_LOC_EXP=0.9, stosowany do locRate production.ts:356, test 1e40@iter0→39.5); paradoxGain=2^k (=31 dla 5 iter); paradoxMult=(1+paradox)^0.5, x_paradox_engine→0.55 (test 16^0.55); iterProdMult=2^(3k) stosowany do globalMultiplier (production.ts:251); ITER_HOLD_S=600. Reset W3 (performIteration): resetuje W2+Equity+equityPerks+insight+drzewko+research/RP, ZACHOWUJE paradox/paradoxItems/kosmetykę/auto-rewrite/story, act→9, x_start_insight=+10% insight (test m10:37-73). PARADOX_ITEMS koszty 5/25/125/10/3/3/3/.../200 + 20 echoes z i18n (test). SAVE_VERSION=1 — pola paradox/paradoxItems/iteration prescaffoldowane, repair-not-reject (spójne z M8/M9).
  GŁÓWNE LUKI: (P0×2) mnożnik kosztów iteracji ×10^(5k) NIE jest stosowany do agentów ani kosztu budowy projektów, a BŁĘDNIE mnoży payout/revenue projektów → brak inflacji kosztów (rdzeń endless) + runaway $. (P1) sloty reguł 2/3 martwe + brak buildera if-then; createRewritePreview alokowany co tick; sim „bez ściany" tautologiczny (wstrzykuje locRate, maskuje oba P0). -->

## MUST FIX (P0/P1 — blokuje merge)
- [x] **P0-1** | `src/systems/production.ts:277` — mnożnik kosztów iteracji `10^(5k)` NIE jest stosowany do kosztu agentów: `cache.costs.generatorMultiplier = effects.generatorCostMultiplier` (tylko redukcje z perków/insightów, init 1), a `iterationCost` (=`calculateIterationCostMultiplier`, :245) trafia WYŁĄCZNIE do `projectMultiplier` (:278); każda ścieżka kosztu generatora (getGeneratorCost :339-340/405, getGeneratorMaxAffordable :444-446) pomija `10^(5k)` — dlaczego: 04 §4 „koszty bazowe **wszystkiego** ×10^(5k)" (ITER_COST_E_PER_K=5); hardware (compute.ts:36) i upgrade'y (upgrades.ts:21) JUŻ to stosują, więc pominięcie dotyczy tylko agentów (główny sink) — przy iter=1 agenci są 10^5× za tani, przy iter=5 (AC1) 10^25× → pętla trywialnie re-clearowana bez „przebudowy strategii" (04 §4); maskowane, bo runEndlessSmokeSim wstrzykuje locRate i nie kupuje agentów — fix: pomnóż koszt generatora przez `calculateIterationCostMultiplier(iteration)` (jak hardware/upgrade'y), np. składając iterationCost do `generatorMultiplier`.
  - fix-note: Applied `iterationCost` to `cache.costs.generatorMultiplier` in `src/systems/production.ts`; covered by `tests/m10-endless.test.ts` ("applies iteration cost to agent and project build costs only").
- [x] **P0-2** | `src/systems/projects.ts:112` + `:258` — mnożnik `10^(5k)` jest po ZŁEJ stronie projektów: `getProjectCost` (standard, :112) zwraca `project.costLoC.copy()` BEZ mnożnika (koszt budowy nie skaluje się z iteracją), a `getBaseProjectPayout` (:255-259) mnoży payout przez `cache.costs.projectMultiplier` (=iterationCost 10^5k) → payout I revenue (:263) rosną ×10^(5k) na iterację — dlaczego: 04 §4 mówi „koszty bazowe wszystkiego ×10^(5k); produkcja bazowa ×2^(3k)" — payouty NIE skalują się iteracją; obecnie projekty kosztują tyle samo, a płacą 10^(5k)× więcej → runaway $ + brak wymaganej inflacji kosztu budowy (hard rule „nie wymyślaj liczb") — fix: zastosuj `iterationCost` do kosztu budowy projektu (getProjectCost), a usuń `projectMultiplier` z payout/revenue (payout skalowany tylko payoutMultiplier/valueRatio).
  - fix-note: Moved `projectMultiplier` onto `getProjectCost` for standard/open-source/refactor costs and removed it from base payout/revenue; covered by `tests/m10-endless.test.ts` ("applies iteration cost to agent and project build costs only").
- [x] **P1-1** | `src/systems/automation.ts:80,172` + `src/data/prestige.ts:484-497` — sloty reguł Paradox 2/3 są MARTWE i brak buildera if-then: `getOwnedParadoxRuleSlots` jest czytane wyłącznie jako bramka boolean `> 0` (unlock :80, tick :172) + jako etykieta UI (main.ts:934/1131); `AutomationRule` (state.ts) ma tylko `enabled` — brak warunku/progu, jedna sztywna reguła auto-REWRITE; x_rule_slot_2 (25) i x_rule_slot_3 (125) nie dodają żadnej reguły — dlaczego: 09 §11 „x_rule_slot_1/2/3 … slot reguły automatyzacji (if-then builder)" + 04 §4.3 „mini-skrypty 'if debt>X then refactor'" + 08 M10 „auto-rewrite rule builder" (zakres M10, nie future) — gracz płaci 150 Paradox za zerowy efekt mechaniczny; i18n (en.json) obiecuje „Adds 1 auto-REWRITE rule slot" per slot — fix: zmapuj liczbę slotów na realne, niezależne reguły if-then (builder), albo min. każdy slot = dodatkowa konfigurowalna reguła.
  - fix-note: Added three independently toggleable auto-REWRITE if/then rules unlocked by slots 1/2/3 at 1x/2x/4x required Insight, and preserved them across ITERATION; covered by `tests/m10-endless.test.ts` ("maps each Paradox rule slot to an independent auto-REWRITE rule").
- [x] **P1-2** | `src/systems/automation.ts:176` — `tickAutoRewrite` woła `createRewritePreview(state)` CO TICK (tickAutomation 10 Hz, main.ts:431) tylko po to, by odczytać `.canRewrite`: preview alokuje 2× `Big.copy()` (loc/money), `createEmptyGeneratorRecord` (pełny rekord per-generator) i 3× `getOwnedInsightEffects` (świeża tablica + iteracja 25 węzłów każda) → ~10-12 obiektów/tick gdy auto-REWRITE włączone — dlaczego: 07 §9 „Mutujące operacje Big w tick (zero GC churn)", „tick < 2 ms"; efekt jest konsumowany (performRewrite), więc to alokacja hot-path, nie martwy efekt — fix: policz `canRewrite` tanio (`calculateAvailableInsightGain >= calculateRewriteRequirement`, same number-y) bez budowania pełnego preview.
  - fix-note: Replaced the per-tick preview build with `calculateAvailableInsightGain` / `calculateRewriteRequirement` numeric checks; covered by `tests/m10-endless.test.ts` ("keeps auto-REWRITE hot path off rewrite preview allocation").
- [x] **P1-3** | `tools/sim/index.ts:159` — `runEndlessSmokeSim` dowodzi AC1 („5 iteracji bez ściany") WSTRZYKNIĘCIEM, nie organicznie: `cache.locRate = getIterationSoftcapThreshold(iteration)` ustawiane wprost co pętlę → `tickIterationHold` zawsze osiąga ITER_HOLD_S, `performIteration` zawsze przechodzi, więc exit-code (:100) i `tests/sim.test.ts:21` (`iteration===5`) są tautologiczne; sim NIE kupuje agentów/projektów, więc NIE wykrywa ściany balansowej (dokładnie to zamaskowało P0-1/P0-2) — empirycznie organiczny `--strategy sane --hours 100` osiąga tylko iterację 1 — dlaczego: 08 M10 AC1 „sim robi 5 iteracji bez ściany" (ściana = przeszkoda progresji, nie tylko overflow); to powtórka lekcji z M9 (wstrzykiwanie stanu zamiast grania nie waliduje AC balansowego) — fix: niech sim endless dochodzi do progu organicznie (kupuj agentów/hardware/projekty pod eskalującymi kosztami ×10^(5k)) przez 5 iteracji i twardo asercjonuj iterację, by sim wychwytywał ściany.
  - fix-note: Removed direct `cache.locRate` threshold injection; endless smoke now seeds a post-finale stress state, buys/ticks eras, hardware, upgrades, research, projects, generators, production, hype, debt, and iteration hold, then asserts 5 ITERATIONs with peak exponent tracking. Covered by `tests/sim.test.ts` and `npm run sim -- --endless-iterations 5`.

## ADVISORY (P2 — nie blokuje)
- [x] **P2-1** | `src/ui/render.ts:1100` — sekcja Paradox/ITERATION (`createParadoxSection`) jest dopisywana do ekranu Rewrite+ BEZWARUNKOWO (brak `hidden` powiązanego z isIterationUnlocked/isParadoxShopUnlocked); pełna prognoza ITERATION (progi softcap, paradoxGain) + cały 27-itemowy sklep Paradox renderują się jako „locked" zaraz po 1. REWRITE (~2,5-3h), choć W3 odblokowuje się dopiero po finale (~50h+) — dlaczego: 06 §4 „taby warstw prestiżu … ukryte do odblokowania"; predykaty unlock istnieją, ale używane tylko do gatingu akcji (canIterate/canExit), nie do ukrycia — fix: ukryj sekcję ITERATION/Paradox (hidden) dopóki `isIterationUnlocked(state)` jest false.
  - fix-note: Added `ParadoxView.unlocked` from `isIterationUnlocked(state)` and hides/unhides the section root in create/update render paths; verified by `npm run check`.
- [x] **P2-2** | `src/systems/prestige.ts:537` — `tickIterationHold` co tick (10 Hz) bezwarunkowo liczy `getIterationSoftcapThreshold(iteration)` → `Big.fromLog10` (świeży Big) PRZED short-circuitem `unlocked`, więc alokuje dla WSZYSTKICH graczy (też pre-finale, iteration=0) cały czas gry — dlaczego: 07 §9 „DerivedCache … przeliczany eventowo, nie co tick", „zero GC churn"; identyczna wartość liczona już eventowo w applyIterationSoftcap; pojedyncza mała alokacja (P2, w skali zaakceptowanych alokacji tickProduction) — fix: early-return `if (!isIterationUnlocked(state)) return false` przed liczeniem progu, lub trzymaj próg w DerivedCache.
  - fix-note: Added early locked return before threshold allocation, preserving stale-hold cleanup if needed; verified by `npm run check`.
- [x] **P2-3** | `tools/sim/index.ts:173` + `tests/sim.test.ts:23` — asercja AC2 (e<9e15 po 100h) jest słaba: `getMaxBigExponent` liczony NA KOŃCU `runEndlessSmokeSim` (po `performIteration`→recompute zerującym locRate) = snapshot PO resecie (max_e=30), nie szczyt biegu (mid-run locRate=threshold(4)=1e95, e=95); dodatkowo smoke biegnie ~50 min (ITER_HOLD_S×5), nie 100h, a kampanijny `--hours 100` (max_e=40) NIE asercjonuje max_e<9e15 (gate sprawdza tylko completeH/missing) — dlaczego: 08 M10 AC2; AC2 jest faktycznie spełnione (margines kosmiczny), ale asercja nie wychwyci regresji szczytu — fix: śledź szczytowy wykładnik w trakcie biegu i asercjonuj <9e15 na realnym horyzoncie ~100h.
  - fix-note: `runEndlessSmokeSim` now tracks peak exponent during the run before reset snapshots; verified by `tests/sim.test.ts`, `npm run sim -- --endless-iterations 5` (`max_e=170`), and `npm run sim -- --strategy sane --hours 100` (`max_e=40`).
- [ ] **P2-4** | `src/systems/prestige.ts:700` — brak mechanizmu wyboru motywu Paradox: `getActiveParadoxTheme` zwraca PIERWSZY posiadany motyw w kolejności PARADOX_ITEMS (crt→glitch→void), brak settera select/setTheme; po kupnie x_theme_crt kupione później x_theme_glitch/x_theme_void nigdy się nie wyświetlą — dlaczego: 09 §11 + 04 §4.3 (3 kosmetyczne motywy terminala); kosmetyka — konsument (UI wyboru motywu) prawdopodobnie polish M11 → P2 — fix: dodaj wybór aktywnego motywu (setter + pole stanu), albo odłóż motywy do M11 z UI wyboru.
  - fix-note: → backlog

## DISPUTED (wypełnia Codex, rozstrzyga człowiek)
_(brak)_

## PLAN-ISSUES (uwagi reviewera do samego planu — nieblokujące)
- **Sprzeczność: czy perki Equity przeżywają ITERATION? (04 §3.3 vs §1).** §3.3 (linia 52): perki Equity „Trwałe na zawsze (przeżywają wszystko poza twardym resetem sava)" — co obejmowałoby ITERATION. §1 (tabela warstw, W3): ITERATION resetuje „wszystko z W2 + Equity", a kolumna „co zostaje" to tylko „Paradox, meta-modyfikatory, kosmetyka" (perków Equity tam nie ma). Kod jest zgodny z §1/M10 (performIteration zeruje res.equity + owned.equityPerks; test m10:64,68), więc to wyłącznie niespójność tekstu planu → doprecyzuj §3.3, że perki przeżywają wszystko poza twardym resetem sava ORAZ ITERATION.

## Runda 2 — weryfikacja (wypełnia reviewer)
- Naprawy zweryfikowane (`npm run check` zielony: 87/87 vitest, validate PASS 51 plików; `npm run sim -- --endless-iterations 5` uruchomiony własnoręcznie → iterations=5, paradox=31, max_e=170, EXIT=0):
  - **P0-1 OK** — `cache.costs.generatorMultiplier = effects.generatorCostMultiplier * iterationCost` (production.ts:277): koszt agentów skaluje się teraz ×10^(5k) (przy iter=0 ×1, bez zmiany); redukcje perków (<1) × iterationCost (≥1) komponują się poprawnie.
  - **P0-2 OK** — `iterationCost` przeniesiony na KOSZT budowy: `getProjectCost` mnoży wszystkie 3 rodzaje przez iterationCost (projects.ts:106-117: standard costLoC×iterCost, open_source/refactor locRate×const×iterCost), a `getBaseProjectPayout`/`getBaseProjectRevenue` (254-259) już NIE używają projectMultiplier → koniec runaway $; mnożnik kosztu na open_source/refactor jest zgodny z 04 §4 „wszystkiego" i nie tworzy ściany (organiczny sim osiąga 5 iteracji).
  - **P1-1 OK** — `AUTO_REWRITE_RULES` (automation.ts:20-30): 3 niezależnie przełączalne reguły auto-REWRITE odblokowywane slotami 1/2/3 (requiredSlots 1/2/3, distinct thresholdMultiplier 1x/2x/4x); tickAutoRewrite (202+) iteruje reguły z gatingiem `ruleSlots >= rule.requiredSlots` + enabled → x_rule_slot_2/3 dają teraz dodatkowe reguły; zachowane przez ITERATION. Test „maps each Paradox rule slot to an independent auto-REWRITE rule".
  - **P1-2 OK** — tickAutoRewrite używa `calculateAvailableInsightGain`/`calculateRewriteRequirement` (number-y, automation.ts:207-208) zamiast `createRewritePreview` → brak alokacji Big/tablic/rekordu per-tick. Test „keeps auto-REWRITE hot path off rewrite preview allocation".
  - **P1-3 OK** — wstrzykiwanie `cache.locRate = getIterationSoftcapThreshold` USUNIĘTE; runEndlessSmokeSim teraz kupuje organicznie (buyBestGenerators :212 + eras/hardware/upgrades/research/projects + tickProduction) i śledzi SZCZYTOWY wykładnik (`Math.max(..., getMaxBigExponent)` w pętli :172) — osiąga 5 iteracji pod realnymi kosztami ×10^(5k) (max_e=170), więc sim wychwyciłby ścianę; oba P0 nie były już maskowane.
  - P2: P2-1 OK (`ParadoxView.unlocked` z isIterationUnlocked, `section.hidden = !view.unlocked` render.ts:1166/2389 → sekcja ITERATION/Paradox ukryta do odblokowania); P2-3 OK (peak-exponent tracking, max_e=170 zamiast snapshotu po-resecie 30); P2-2 OK (early-return przed alokacją progu, per fix-note + check zielony); P2-4 → backlog (wybór motywu, świadomie, advisory).
- Nowe P0 wprowadzone poprawkami: brak. Sprawdzone: kompozycja generatorMultiplier (redukcja × iterationCost) poprawna; payout projektów już bez iterationCost (koniec runaway $); koszt open_source/refactor ×iterCost zgodny z planem i bez ściany (sim 5 iter); nowe reguły auto-REWRITE additive w state.automation (bez zmiany kształtu AutomationRule → bez migracji); hot-path bez alokacji; organiczny sim 5 iteracji + max_e=170 ≪ 9e15. 87/87 testów + validate PASS.
- Decyzja: DONE — oba P0 (koszty iteracji: agenci + projekty) i wszystkie P1 (sloty reguł, hot-path, organiczny sim) rozwiązane i zweryfikowane (kod + organiczny sim + testy); poprawki bez nowych P0. P2-4 (wybór motywu) świadomie w backlogu. Sprzeczność planu (perki Equity vs ITERATION) pozostaje jako PLAN-ISSUE do decyzji człowieka (nieblokująca).
